# STORY_FLOW_PLAN — ניסוי UX מקביל: «הלקוח מספר. Aperta מתרגמת.»

מסמך התכנון של הניסוי, לפני הקוד. הוא מתאר **מה נוצר, מה משתנה, למה, ואיך מוחקים
את הכול**. הכלל העליון: זהו מסלול מקביל. דף הבית הקיים, `/design`, העורך, ההזמנה,
ה-checkout וצינור היצירה ממשיכים לעבוד בדיוק כפי שהם, וברירת המחדל של המערכת אינה
משתנה בשום נקודה.

---

## 0. מה כבר קיים בריפו (הסקירה שקדמה לתכנון)

| מה | איפה | מה נלמד |
| --- | --- | --- |
| דף הבית | `src/app/(site)/page.tsx` | Hero + רצועת מניפסט, CTA יחיד ל-`/design`. נשאר כפי שהוא. |
| המסע הקיים | `src/app/(site)/design/page.tsx` (1,497 שורות) | מתזמר את כל המסכים: `product → sizes → brief → processing → result → summary → checkout → done`, כולל שער חשבון, שחזור מ-OAuth, `jobId` שורד-ניתוק, Back, שמירה מקומית ושיתוף. |
| מסכי המסע | `src/components/create/*` | `ProductScreen`, `SizesScreen`, `BriefScreen`, `ProcessingScreen`, `ResultScreen`, `SummaryScreen`, `CheckoutScreen`, `DoneScreen`, ו-`ui.tsx` (פרימיטיבים: `PrimaryBtn`, `OptionBtn`, `FieldLabel`, `Modal`…). |
| מודל המסע | `src/components/create/model.ts` | `CreateState`, `WIDTH`, `circumferenceMm`, `stripLengthMm`, `sizeIssue`, `CIRC_LIMIT_MM`, `buildPrompt`, `frameWidthMm`. |
| מידות | `src/lib/sizing.ts`, `src/lib/fabrication.config.ts` | `defaultWidthMm` 18 (צמיד) / 6 (טבעת); `widthRangeMm` `[5,80]` / `[4,18]`; ההמרה היקף → אורך פריסה עוברת ב-`computeSizing`. |
| פרומפט היצירה | `src/lib/llm/imagegen.ts#buildRenderPrompt` | פסקת `PROPORTIONS` מכתיבה אורך, רוחב ויחס. |
| מסגור / vectorizer | `src/lib/vectorizer.ts` → `src/lib/geometry/frameCutouts.ts` | קנה מידה אחיד מתקבל **רק** אם הרוחב האחיד נופל בתוך `WIDTH_TOLERANCE` (5%) מהמוזמן; אחרת הרוחב נצמד למוזמן והפער נמתח אופקית (`stretch`). אותו קובץ רץ גם ב-`geometry-service` (Hetzner) וגם ב-`workers/frame`. |
| שפת המותג | `src/app/globals.css`, `src/app/layout.tsx` | טוקנים: porcelain / porcelain-slab / graphite / lapis / mist / ink60 / ink80. פונטים: Archivo (display) + Assistant (body). לוגו: `components/site/Wordmark`, `BrandMark`. נכסים: `bracelet-hero.webp`, `ring-hero.webp`, `hand-wrist.webp`, `hand-ring.webp`, ואיורי `PatternMark`. |

**מסקנה שקבעה את הארכיטקטורה:** התזמור של המסע אינו מסך — הוא 1,497 שורות של
טיפול במצבי קצה אמיתיים (ניתוק באמצע יצירה, חזרה מגוגל, Back, שכפול הזמנה).
שכפול שלו למסלול Story היה מייצר צינור שני שנשאר תואם רק בזכות משמעת. לכן Story
**נכנס לאותו תזמור** מאחורי דגל, ולא לצידו.

---

## 1. הארכיטקטורה שנבחרה

```
/story              ← דף בית חלופי (סטטי, שפת מותג קיימת)
   ↓ CTA
/story/create       ← מסך יצירה פשוט: מוצר · סיפור
   ↓ CTA "לתרגם את הסיפור שלי"
   ↓ (handoff ב-sessionStorage)
/design?story=1     ← המסע הקיים, במצב story: קופץ ישר ל-processing
   ↓
תוצאה → "להזמין" → **מידות** → סיכום → checkout → done
                     ↑ אותו מסך מידות של המסלול הרגיל, אחרי הבחירה
```

- **אין generator שני.** `/api/generate` הקיים, אותו פרומפט, אותו צינור.
- **אין עורך שני.** הקישור המשני ב-`/story/create` פותח את `/design` הקיים, על כל
  הפקדים שלו, עם prefill של מה שכבר מולא.
- **אין schema change ואין migration.** מצב Story נוסע בבקשה (`mode: "story"`),
  לא בעמודה.

---

## 2. קבצים חדשים

| קובץ | תפקיד |
| --- | --- |
| `STORY_FLOW_PLAN.md` | המסמך הזה. |
| `src/i18n/story.ts` | **כל** הקופי של הניסוי, בנפרד מ-`he.ts` — מחיקת הניסוי היא מחיקת הקובץ. |
| `src/app/(site)/story/page.tsx` | דף הבית החלופי (Server Component + metadata). |
| `src/app/(site)/story/create/page.tsx` | עטיפת metadata למסך היצירה. |
| `src/components/story/StoryHome.tsx` | ה-Hero, רצועת ארבעת השלבים וה-CTA. |
| `src/components/story/StoryExamples.tsx` | הדוגמה החיה: סיפור ↓ תרגום, מתחלפת בין 4 דוגמאות (Client). |
| `src/components/story/StoryCreate.tsx` | מסך היצירה: מוצר · סיפור · CTA · לינק לעורך (Client). |
| `src/lib/client/storyHandoff.ts` | העברת `{product, story}` מ-`/story/create` ל-`/design` (sessionStorage). |
| `src/lib/story/mode.ts` | שרת: `STORY_MODE`, `storyFrameDims()` — גזירת הרוחב מהיחס שהמודל צייר, בתוך טווח הייצור. |
| `src/lib/story/prompt.ts` | פרומפט שלב אחד: סיפור → צורה מופשטת. נפילה־לאחור בלבד מאז §4.7. |
| `src/lib/story/designStage.ts` | שלב הטקסט: הפרומפט שמתרגם סיפור לשלושה כיווני עיצוב, הפרומפט שמבצע אותם, והפענוח. ראה §4.7. |
| `src/lib/story/__tests__/storyFrame.test.ts` | טסטים: קנה מידה אחיד, רוחב שנגזר בלי צביטה, אי-פגיעה במסלול הקיים. |
| `src/lib/story/__tests__/storyPrompt.test.ts` | טסטים לפרומפט: הסיפור נכנס, המוצר הנכון, מספר הפריטים מהתכנון, ומה שהווקטורייזר צריך. |
| `src/lib/story/__tests__/storySizeStep.test.ts` | טסטים לסדר החדש: הסרגל, יצירה לפני שיש מידה, ומסגור מחדש כקנה מידה אחיד. |
| `src/lib/story/__tests__/designStage.test.ts` | טסטים לשלב העיצוב: ההצבות, הפענוח, והנפילה־לאחור. |
| `src/lib/story/__tests__/storyRender.test.ts` | טסט חוצה-שפות: מה ש-forme שולחת נמצא ברשימות המותרות של הקופסה. |

---

## 3. קבצים קיימים שישונו — ולמה כל שינוי הכרחי

| קובץ | השינוי | למה |
| --- | --- | --- |
| `src/components/create/model.ts` | שדה `story: boolean` ב-`CreateState` + `false` ב-`INITIAL`; `resizing: boolean`; `STORY_RAIL` / `railFor` / `railIndex`. | הדגל חייב לנסוע עם המצב: `stashCreateState` שומר את כל ה-state בדרך לגוגל וחזרה, ובלעדיו מי שנדרשת להזדהות באמצע חוזרת למסלול הישן. הסרגל הנפרד הוא §4.1: המידה זזה, והסדר חייב להיות מוכר לניווט ולא רק לתצוגה. |
| `src/app/(site)/design/page.tsx` | קריאת `?story=1` בכניסה (אותו אפקט שכבר מטפל ב-`?resume`/`?from`/`?designs`), ושליחת `mode` ליצירה ולבחירת הצעה. | זו נקודת החיבור היחידה בין המסלולים. בלי `story=1` בכתובת — התנהגות זהה לחלוטין לקודם. |
| `src/lib/client/api.ts` | `generate()` ו-`chooseCandidate()` מקבלים `mode?: "story"` אופציונלי. | להעביר את הדגל לשרת בלי לשנות אף חתימה קיימת. |
| `src/app/api/generate/route.ts` | `mode: z.enum(["story"]).optional()`; ב-story: מסגור כל מועמד למידות שנגזרו ממנו, עדכון רוחב העיצוב לזוכה, וטווח רוחב בפרומפט. | זהו הבידוד עצמו — כל הענף מאחורי `if (story)`. |
| `src/app/api/designs/[id]/choose/route.ts` | אותו `mode` אופציונלי; ב-story הדוגמה נמסגרת למידות של עצמה. | בלי זה מעבר להצעה אחרת היה מותח אותה לרוחב של ההצעה הזוכה — כלומר בדיוק העיוות שהמסלול בא למנוע. |
| `src/lib/llm/imagegen.ts` | פרמטר אחרון אופציונלי `widthRange?: [number, number]`; כשהוא קיים, פסקת `PROPORTIONS` אומרת אורך מדויק + טווח רוחב. | החלטת המוצר: אין רוחב קבוע — הרוחב הוא של העיצוב, בתוך טווח הייצור. בלי הפרמטר הפסקה נבנית בדיוק כמו היום. |
| `src/lib/db/designs.ts` | `updateDesignWidth()` חדש (UPDATE על עמודה קיימת). | כדי שהרוחב שנגזר יהיה מקור אמת אחד: הזמנה, תמחור, הדמיה ועריכה עתידית. אין עמודה חדשה ואין מיגרציה. |
| `src/lib/db/runs.ts` | שדה `mode?: "story"` אופציונלי ב-`RunInputs` (jsonb, בלי מיגרציה). | הניסוי נועד להימדד. בלי השדה הזה הרצת Story נראית ביומן זהה לכל הרצה אחרת, ואי אפשר להשוות בין המסלולים — לא בהצלחה, לא במתיחה ולא ברוחב שיצא. |
| `src/components/create/ResultScreen.tsx` | כשה-state ב-story — כותרת ומשפט מסגור אחרים. | §17: אותו מסך, מסגור טקסטואלי אחר. שתי שורות בתנאי. |
| `src/components/create/SizesScreen.tsx` | ב-story: בלי בורר הרוחב ובלי תצוגת הרוחב. | §4.1 ו-§4.2: הרוחב נגזר מהעיצוב שכבר נבחר, ומחוון שמזיז אותו מבטיח שליטה שאינה קיימת. שאר המסך — פריסטים, מידה מדויקת, ישיבה, מדריך — זהה. |
| `src/components/create/ui.tsx` | `StepRail` מקבל `rail` אופציונלי. | ברירת המחדל היא `RAIL`, כלומר כל מי שלא מוסר כלום מקבל בדיוק מה שהיה. |
| `src/app/sitemap.ts` | הוספת `/story`. | לפי ההחלטה: מוסתר בניווט, פתוח לאינדוקס. |

**לא משתנים:** `src/app/(site)/page.tsx`, `SiteHeader`, `SiteFooter`, `robots.ts`,
`frameCutoutsDims`, `frameClient`, `workers/frame`, `geometry-service`,
`vectorizer.ts`, מסלול ההזמנה, `checkout`, ה-DB.

---

## 4. ההחלטות שנקבעו מראש (ולמה)

### 4.1 המידה — אחרי התרגום, לא לפניו
**עודכן.** בגרסה הראשונה המידה נשאלה במסך היצירה, לפני שהמשתמשת ראתה צורה אחת.
זו הייתה השאלה היחידה שעמדה בין הסיפור לתשובה, ואין לה עדיין הקשר: מדידה של פרק
יד לפני שראית תרגום אחד היא בקשה למספר בלי סיבה. מכאן היא זזה ל**שלב ההזמנה**:
הלקוחה מספרת → מקבלת תרגומים → בוחרת → לוחצת "להזמין" → ורק אז נשאלת למידה.

מה שהיא רואה שם הוא **מסך המידות של המסלול הרגיל** על כל מה שבו: הפריסטים
הקבועים מראש (קטן/בינוני/גדול), שדה המידה המדויקת, בורר הישיבה (בצמיד) ומדריך
המדידה. מה שאין שם הוא בורר הרוחב ותצוגת הרוחב — במסלול הזה הרוחב נגזר מהעיצוב
שכבר נבחר (§4.2), ומחוון שמזיז אותו היה מבטיח שליטה שאינה קיימת.

ההמרה לאורך פריסה עוברת ב-`stripLengthMm()` הקיים, עם `fit: "regular"` (ברירת
המחדל של המערכת) — אין מודל מידות חדש, אין טווחים חדשים, ו-`sizeIssue()` הקיים
הוא מי שחוסם מספר שאינו מדידה.

**מה זה דורש מהצינור.** עד שנבחרה מידה, המסע עובד עם ברירת המחדל של המערכת
(הפריסט הבינוני של `INITIAL`) — כלומר היצירה רצה על אורך אמיתי ולא על אפס.
האורך הזה הוא גם מה שהגרסה נשמרה בו, ולכן בסיום מסך המידות העיצוב **ממוסגר
מחדש** לאורך שנמדד: אותו SVG, דרך אותו מסלול שבחירת הצעה עוברת בו
(`/api/designs/[id]/choose` במצב story), שגוזר את הרוחב מהיחס שצויר ומחזיר קנה
מידה אחיד. זו הגדלה/הקטנה של אותה צורה — לא יצירה חדשה, ולא מתיחה בציר אחד.
מכאן גם ש-`setSizes` **אינו מבטל** כאן את העיצוב כפי שהוא עושה במסלול הרגיל:
שם המידה קדמה ליצירה, וכאן היא באה אחריה.

**סדר השלבים בסרגל** משתנה איתה: `STORY_RAIL` (עיצוב → תוצאה → מידות → סיכום →
תשלום) במקום `RAIL`. הסרגל הוא גם ניווט, ולחיצה על "מוצר" במסלול שאין בו מסך
מוצר הייתה מוציאה מהמסלול בלי דרך חזרה.

### 4.2 הרוחב — אין בחירת רוחב, ואין גם רוחב קבוע
לפי ההחלטה: הרוחב אינו נבחר על ידי המשתמשת **ואינו קבוע מראש** — הוא של העיצוב.
מה שקורה בפועל:

1. הרשומה נוצרת עם רוחב ברירת המחדל הקיים (18 / 6). הוא משמש **לתכנון בלבד** —
   בחירת הקנבס ומספר הפאנלים ב-`planRender`. הוא **אינו** נכנס לפרומפט.
2. הפרומפט ביצירה מאפס אינו אומר מידות בכלל (§4.7) — קנה המידה הוא של המודל.
3. אחרי הווקטוריזציה, הרוחב **נגזר** מהיחס שהמודל צייר בפועל. וזהו.

**הצביטה לטווח הייצור הוסרה** (החלטת גל). קודם הרוחב שנגזר נצבט ל-
`FAB.products[t].widthRangeMm` (צמיד 5–80, טבעת 4–18), ומחוץ לטווח הצביטה
הייתה מחזירה בדיוק את המתיחה האופקית שהמסלול הזה בא למנוע. נמדד: בצמיד היא
כמעט לא נגעה, אבל **בטבעת כל יחס מתחת ל-3.3 נצבט** — עד ×1.33 מתיחה על צורה
שהמודל צייר נכון, ובתחום שהוא באמת מצייר.

מה ששומר על הייצור הוא מה שתמיד שמר עליו: הוולידציה, שרצה על הגאומטריה שאחרי
המסגור ופוסלת מה שאי אפשר לחתוך. פסילה של מועמד היא תשובה טובה יותר מפריט
מעוות שעובר בשקט.

טווח הרוחב עדיין נמסר לפרומפט ב**עריכה ובכיתוב** (שם `buildRenderPrompt` עדיין
מתאר פריט במידות), והוא נפתח שם כדי להכיל את הרוחב בפועל של הפריט שנערך — אחרת
המשפט סותר את עצמו: "ברוחב 4 עד 18 מ"מ, כשבערך 24 מ"מ הוא הרגיל".
ראה `widthRangeOf`.

### 4.3 יחס הצדדים — למה זה לא נוגע בקוד הגיאומטרי
`frameCutoutsDims` כבר לוקח קנה מידה **אחיד** כשהרוחב האחיד נמצא בתוך 5% מהמוזמן.
לכן ב-story אנחנו לא משנים אותו — אנחנו מוסרים לו רוחב מוזמן ש**שווה** לרוחב
האחיד:

```
uniform = drawnWidth × (orderedLength / drawnLength)
ordered = uniform                          ← מה שנמסר למסגור
```

`uniform === ordered` תמיד → `stretch = 1.000`, אפס מתיחה, אפס עיוות, בכל יחס
שהמודל יצייר. העיגול לשתי ספרות שנשאר שם הוא עיגול ולא צביטה: הפער נמצא הרבה
מתחת ל-`WIDTH_TOLERANCE`, ולכן המסגור בוחר את הרוחב האחיד ממילא.

**זה כל השינוי בהתנהגות הוקטור.** אין דגל שנוסע ל-geometry-service, אין שינוי
בקובץ הגיאומטריה, ולכן אין תלות ב-redeploy של הקופסה — ובעיקר: אין שום נתיב שבו
המסלול הקיים מקבל התנהגות אחרת.

### 4.4 מגוון צורני בין ההצעות
במסלול הרגיל הלקוחה ביקשה עיצוב מסוים, והדירוג שואל "מי הכי קרוב למה שהוזמן".
במסלול Story השאלה היא אחרת — "איזו מהפרשנויות הכי שלי" — ולשאלה הזו ארבע הצעות
שנראות אותו דבר הן אשליה של בחירה. לכן, ב-Story בלבד:

- **הפרומפט** מבקש במפורש קריאות שונות של אותה כוונה: צללית שונה, חיתוך שנע בין
  פתוח לצפוף, קצב סדיר מול לא־סדיר. משפט אחד, ורק כשיש יותר מפריט אחד ולא
  בעריכה.
- **סדר ההצעות** נקבע ב-farthest-first על שני צירים שכבר מחושבים על כל מועמד:
  `openAreaPct` (כמה פתוח) ו-`drawnRatio` (איזו פרופורציה — ובמסלול הזה גם הרוחב
  שייצא). **ההצעה הראשונה אינה משתנה** — היא נבחרת כמו קודם ונשמרת כגרסה; מה
  שמשתנה הוא הסדר של השאר.

### 4.5 מחיר קבוע למוצר — בשני המסלולים
המחיר נשא שתי תוספות משתנות: `widthAdd` (₪5 למ"מ בצמיד, ₪14 בטבעת מעל ברירת
המחדל) ו-`complexity` (‎−45/0/+65 לפי הצפיפות). **שתיהן הוסרו, בשני המסלולים**
(החלטת גל): צמיד ₪399, טבעת ₪299, ועוד ₪35 משלוח — תמיד.

הנימוק: המחיר שכרטיס המוצר מבטיח היה נכון רק לפריט בברירת המחדל, וכל בחירה
אחרת גילתה מספר אחר בסוף המסע. במסלול Story זה הפך לבלתי אפשרי ממש — שם הרוחב
אינו נבחר אלא נגזר מהעיצוב שחזר, כלומר התוספת הייתה נגבית על משהו שהלקוחה לא
ביקשה ולא יכלה לצפות.

**הפקדים לא הוסרו:** בורר הרוחב והמאפיינים במסלול הרגיל נשארים כפי שהיו ומעצבים
את הפריט — הם פשוט אינם מזיזים את המחיר.

זהו שינוי במסלול הקיים, ולכן הוא **מחוץ** לגבולות הניסוי: הוא אינו מוסר עם
מחיקת ה-story ואינו מסומן `story mode`.

### 4.5.1 הרוחב שנגזר חוזר אל המצב
מרגע שיש גרסה, הרוחב במצב מתעדכן מה-viewBox שלה. לא בשביל המחיר (הוא קבוע) אלא
בשביל שני דברים שקוראים את המצב: הכרטיס ב"העיצובים שלי", ותוספת הנוחות שבתוך
`stripLengthMm` — שנגזרת גם מהרוחב, ובטבעת עולה במדרגות.

### 4.6 מה נשאר של המסלול הישן
`/design` בלי `?story=1` אינו עובר באף אחד מהענפים האלה: `mode` לא נשלח,
`storyFrameDims` לא נקרא, הפרומפט של `lib/story/prompt.ts` לא נבנה, הסרגל הוא
`RAIL`, `setSizes` ממשיך לבטל עיצוב על שינוי מידה, ו-`ResultScreen` מציג את
הכותרת הקיימת. בורר הרוחב, ה-presets, המאפיינים והעורך — כולם במקומם.

### 4.7 שני שלבים: סיפור → מפרט עיצוב → תמונה

**מה שנראה על המסך, ולמה.** בגרסה הראשונה הסיפור הלך ישירות למודל התמונה,
ושלוש ההצעות חזרו כמלבן שחור עם חיתוך רחב אחד, נבדלות בקושי, בלי שהצללית
החיצונית תשתתף — בזמן שהפרומפט מבקש במפורש את שניהם.

הסיבה היא שמודל תמונה התבקש לעשות שני דברים בבת אחת: **לפרש** סיפור לצורה,
ו**לצייר** אותה בדיוק. את הראשון הוא עושה רע. את השני הוא עושה טוב, אם אומרים
לו בדיוק מה לצייר.

מכאן הפיצול (`src/lib/story/designStage.ts`):

```
סיפור → [מודל טקסט] → JSON: שלושה כיווני עיצוב גיאומטריים → [מודל תמונה] → תמונה
```

1. **שלב העיצוב** — `gpt-5.6-luna` ב-`reasoning: medium`, דרך הלקוח הקיים
   (`lib/llm/openai.ts`). הוא מקבל את הסיפור, את המוצר ואת האורך, ומחזיר JSON
   עם שלושה כיוונים, לכל אחד `outer_silhouette`, `metal_structure`,
   `negative_space`, `rhythm_balance`, `manufacturability` ו-`image_instruction`
   — הוראת ציור גיאומטרית, בלי שפה רגשית.
2. **שלב התמונה** — מקבל את ה-JSON הזה במקום את הסיפור, ומתבקש **לבצע** ולא
   לפרש: "DO NOT reinterpret the original user story. DO NOT invent three new
   designs."

**הרוחב אינו נמסר לשלב 1.** האורך כן — הוא מדידה, והיחס בין השניים הוא מה
שהפרומפט צריך. הרוחב הוא של העיצוב (§4.2), ומסירתו הייתה מקבעת בדיוק את מה
שהמסלול משאיר פתוח.

**מה שקבוע בשני הפרומפטים, ולכן קבוע גם בתכנון:** שלושה פריטים, בשלוש שורות,
בתמונה אחת לרוחב. `planRender` מחזיר ממילא 3×1 לשני המוצרים במידות ברירת המחדל
(יחס 8.9 מול יחס טבעי 9.05 בשלוש שורות), והקיבוע רק מונע פער אם משהו מהם יזוז.

### 4.7.2 קנה המידה של הציור הוא תקציב הפיקסלים של הצינור

**עודכן (14.8, עיצוב 165).** הגרסה הראשונה של הפרומפט לשלב 2 אמרה על גודל
הפריט בתמונה *"**may** span almost the full available width… generous white
space is acceptable"* — כלומר הפכה לרשות את מה שהפרומפט של שלב אחד
(`story/prompt.ts`) דורש: *"spanning almost the full width of the image"*.

זו לא הייתה בחירה קוסמטית. הציר הארוך של הפריט מצויר בתוך 1536 פיקסלים של
הקנבס, ומכאן כל השאר:

| הפריט מצויר ב… | px/mm על צמיד 160 מ"מ | פתח 0.5 מ"מ |
| --- | --- | --- |
| 90% מהרוחב | 8.6 | 4.3 px |
| 70% מהרוחב | 6.7 | 3.4 px |
| 60% מהרוחב | 5.8 | 2.9 px |

הרצפה מתועדת ב-`render/canvas.ts` ולא הומצאה כאן: **6 px/mm**, כי הפתח הקטן
ביותר שהמסלול מרשה צריך שלושה פיקסלים כדי לשרוד את המעקב. מתחת לזה ההדמיה
עדיין נראית מצוין לעין — ה-conditioning הוא שמבינר בסף 0.5, ממלא כל חור מתחת
ל-`min_frac` ושובר את הגשרים הדקים. הצינור מדווח על זה כ"הווקטורייזר לא אישר",
והמסך מציג הצעה אחת במקום שלוש.

לכן פסקת `SCALE` בפרומפט חזרה להיות **דרישה**, עם שני הצדדים שלה: הפריט משתרע
כמעט על כל רוחב התמונה, והמקום העודף בשורה מתמלא בלבן מעל ומתחת — לא בהעבה של
הפריט עצמו. `designStage.test.ts` שומר על שניהם.

**מה שאינו ידית:** מספר השורות. הציר הארוך מוגבל ברוחב הקנבס ולא בגובה התא,
ולכן ‎px/mm זהה ב-3×1 וב-1×1; שורה אחת רק מוסיפה לבן, במחיר של חלופה אחת במקום
שלוש. הידית שנשארה, אם עדיין גס, היא `STORY_RENDER.quality`.

**כשזה נכשל, המסלול ממשיך.** מודל טקסט שנפל, פג, או החזיר JSON שאי אפשר לקרוא
— כל אלה מחזירים `null`, והצינור נופל חזרה לפרומפט של שלב אחד (§4.7.1), שהוא
מה שרץ עד עכשיו. יצירה לא נכשלת בגלל השלב הזה, ומה שקרה נרשם ב-`designStage`
ביומן.

**מזהה ההרצה מול הקופסה נגזר מהקלט היציב ולא מהפרומפט.** הפרומפט של שלב 2 נושא
בתוכו את ה-JSON, שנוצר מחדש בכל ניסיון ולעולם אינו זהה — ובלי `requestKey`
(‏`lib/render/service.ts`) כל ניתוק היה קונה רנדר שני. עם המפתח, ניסיון חוזר
מגיע לאותו job והקופסה מחזירה את מה ששולם.

**העלות.** שלב הטקסט הוא בערך ‎$0.002 להרצה (‏1,200 טוקני קלט, 1,500 פלט,
ב-‎$0.20/$1.20 למיליון) — זניח מול קריאת התמונה, וזה גם מה שמאפשר לחזור למודל
התמונה הקטן: אם ההנחיות מדויקות, אין למה לשלם על מודל גדול (§4.2 → `STORY_RENDER`).

### 4.7.1 הפרומפט של שלב אחד — הנפילה־לאחור
במסלול הרגיל הפרומפט נבנה סביב **הזמנה**: הלקוחה בחרה מוצר, מידה, רוחב
ומאפיינים, והפרומפט מוסר אותם למודל כאילוצים. הניסוח הזה מקבל בחזרה וריאציות
על אותו פס — וזה נכון שם, כי שם באמת הוזמן פס מסוים.

במסלול Story השאלה אחרת: מה שמתבקש הוא **תרגום** של סיפור לצורה, והבחירה בין
הפרשנויות היא המוצר. לכן ליצירה מאפס יש שם טקסט משלה
(`src/lib/story/prompt.ts`) ולא פרמטר נוסף על הקיים. מה שהוא אומר:

- **הסיפור הוא הקלט** — הוא נכנס כמו שנכתב, עם הוראה מפורשת לתרגם אותו לקצב,
  גיאומטריה, חלל שלילי, פרופורציה, מתח, חזרה, הפרעה, תנועה, צפיפות ואסימטריה —
  ולא לאייר אותו, ולא להניח עליו אובייקטים או סמלים מזוהים.
- **פרופורציה איכותית, בלי מספרים** — "פרופורציות אמינות של צמיד פתוח / טבעת
  פתוחה", בלי אורך, בלי רוחב ובלי יחס במ"מ. זה מה שמאפשר ל-§4.1 לעבוד: המידה
  נכנסת אחרי התוצאה, במסגור.
- **ייצור** — גיליון אחד, חיתוך לייזר, גלגול/כיפוף וגימור; בלי הלחמה, ריתוך,
  אבנים, ציריות, תוספות או חלקים מנותקים; בלי גשרים דקיקים וחורים זעירים.
- **סגנון ויזואלי** — שחור מלא על לבן טהור, שטוח, אורתוגרפי, בלי צל, גרדיאנט,
  השתקפות או עומק. זו לא העדפה: זה בדיוק מה שהווקטורייזר צריך כדי להפריד מתכת
  מרקע (`color_key="dark"`).
- **מספר הפריטים והפריסה** — מגיעים מ-`planRender` ולא מהטקסט, כי הם מה שנחתך
  בפועל. מספר אחר בפרומפט היה מייצר תמונה שחלקה נזרק או פסים חתוכים. גם צורת
  הקנבס שנאמרת (לאורך / לרוחב) היא זו שתתבקש בפועל מ-`canvasFor`.
- **מגוון** — פסקת `DESIGN VARIATION` מבקשת קריאות שונות של אותה כוונה, ורק
  כשיש יותר מפריט אחד.

**מה לא עובר שם.** בקשת שינוי וכיתוב. באלה התמונה המצורפת היא העיצוב הקיים (או
הכיתוב שכבר נחתך), והפרומפט חייב לדבר עליה ולשמר אותה — וזה מה ש-`buildRenderPrompt`
עושה במצב `editing`/`letteringReference`. שניהם ממשיכים לקבל את טווח הרוחב
(§4.2) בדיוק כמו קודם.

⚠ **מה שיצא ולא הוחלף:** מינימומי הייצור במ"מ (`minHole`, `minLetterBridgeMm`),
שהפרומפט הקיים מוסר כמספרים. במקומם יש כאן ניסוח איכותי ("בלי גשרים דקיקים,
חורים זעירים, מיקרו-פירוט מוגזם"). הוולידציה עצמה לא זזה — היא ממשיכה לאכוף את
אותם מספרים ולפסול מועמד שלא עומד בהם — ולכן זה סיכון לשיעור הפסילה, לא לאיכות
מה שנחתך. אם היחס יורע, החזרה היא שורה אחת ב-`prompt.ts`.

---

## 5. איך מוחקים את הניסוי וחוזרים למצב הנוכחי

**מחיקה מלאה (אין מיגרציה להריץ, אין נתונים לנקות):**

```bash
rm -rf src/app/\(site\)/story src/components/story src/lib/story \
       src/lib/client/storyHandoff.ts src/i18n/story.ts STORY_FLOW_PLAN.md
```

ואז החזרת תשעת השינויים המסומנים בסעיף 3. כל אחד מהם מסומן בקוד בהערה
`story mode` כדי שאפשר יהיה לאתר אותם:

```bash
grep -rn "story mode" src/
```

**או, בקצרה:** כל הניסוי יושב על ענף אחד ואפשר להסיר אותו ב-`git revert` של
ה-merge commit. אין מצב ביניים שדורש טיפול: עיצובים שנוצרו במסלול Story הם
עיצובים רגילים לכל דבר — שורת `designs` עם אורך, רוחב, פער ועובי, וגרסאות
תקינות — והם ימשיכו להיפתח, להיערך ולהיות מוזמנים במסלול הרגיל גם אחרי שהניסוי
יימחק.

---

## 6. Definition of Done — לבדיקה בסיום

- [ ] `/story` — viewport אחד, מובייל ודסקטופ, בשפת המותג הקיימת.
- [ ] המסר Story → Translation → Choice → Jewelry מובן תוך שניות, בלי הסבר על AI.
- [ ] לפחות דוגמה ויזואלית אחת של סיפור → תכשיט (מתחלפת).
- [ ] `/story/create` — טבעת/צמיד, textarea, CTA אחד ראשי, לינק משני לעורך, **בלי בורר רוחב ובלי שדה מידה**.
- [ ] היצירה רצה בצינור הקיים; הרוחב נגזר; היחס נשמר.
- [ ] "להזמין" מהתוצאה פותח את מסך המידות — עם הפריסטים הקבועים מראש — ורק אחריו הסיכום.
- [ ] המידה שנבחרה שם ממסגרת את העיצוב מחדש לאורך שנמדד, בקנה מידה אחיד, בלי לאבד את התרגום שנבחר.
- [ ] `/`, `/design`, העורך ו-checkout — ללא שינוי התנהגות.
- [ ] lint · typecheck · tests · build עוברים.
