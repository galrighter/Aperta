# סטטוס בנייה — סטודיו שלב 1

מסמך מעקב לפי סדר השלבים ב-`docs/BUILD_SPEC_studio_phase1.md` סעיף 14.

| שלב | תיאור | סטטוס |
|---|---|---|
| 0 | שלד ותשתית (Next.js, Cloudflare, Supabase, קונפיג ייצור) | ✅ קוד מוכן — ממתין להשלמת secrets ומיגרציה (ראו למטה) |
| 1 | שלד UI (מסך סטודיו, פרופילים, CRUD עיצובים) | ✅ |
| 2 | יצירה ראשונה (חוזה SVG, נרמול, generate) | ✅ |
| 3 | מנוע ולידציה V1–V10 + לולאת תיקון | ✅ (18 טסטים) |
| 4 | עריכה וסימונים (annotation layer + קומפוזיט) | ✅ |
| 5 | הדמיה תלת-ממדית | ✅ |
| 6 | ייצוא DXF+SVG (כולל אימות ezdxf בטסט) | ✅ |
| 7 | טקסט/פונט סטנסיל, ניווט גרסאות, הקשחה | ✅ text-to-path עם Secular One + גישור אוטומטי; ניווט גרסאות; rate limit; מצבי קצה |

**הערה:** כל הקוד בנוי ועובר בילד + 24 טסטים (כולל round-trip של DXF מול ezdxf
ואימות שאותיות עבריות סגורות לא נופלות). קריטריוני הקבלה שדורשים דיפלוי חי
(עמוד נטען ב-URL ציבורי, בדיקה מהנייד) ממתינים להשלמת ה-secrets והמיגרציה למטה.

## מה נדרש ממך (גל) כדי שהאתר יעלה

1. **מיגרציית DB** — אפשרות א': להוסיף secret בשם `SUPABASE_DB_URL`
   (Supabase → Settings → Database → Connection string URI) ואז להריץ את
   ה-workflow "Apply Supabase migrations" — ירוץ גם אוטומטית על push.
   אפשרות ב': להדביק את `supabase/migrations/0001_init.sql` ב-SQL Editor.
2. **Secrets לפריסה** (Settings → Secrets and variables → Actions):
   - `SUPABASE_URL`, `SUPABASE_SECRET_KEY` — ✅ קיימים לפי מה שמסרת
   - `ANTHROPIC_API_KEY` — נדרש ליצירת עיצובים
   - `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` — נדרשים לפריסת ה-Worker
3. אחרי merge ל-main — ה-workflow "Deploy to Cloudflare" יפרוס את האתר
   ויעדכן את ה-secrets של ה-Worker אוטומטית.

ה-bucket בשם `studio` ושורות ששת הבודקים נוצרים אוטומטית בקריאה הראשונה
לשרת — אין צורך בהקמה ידנית מעבר למיגרציה.

> **סעיף 1 נסגר (27.7).** `SUPABASE_DB_URL` הוגדר עם מחרוזת ה-**session
> pooler**, וה-workflow "Apply Supabase migrations" עובר מאז בכל push ל-main
> ומריץ את כל `supabase/migrations/` בסדר שמות. חמש ההרצות שקדמו לו נכשלו על
> החיבור הישיר `db.<ref>.supabase.co`, שהוא IPv6-only — runners של GitHub
> Actions לא מגיעים אליו.

## פתוח — דחוי

**[`docs/TODO.md`](docs/TODO.md)** מרכז את מה שנשאר, עם מה שנמדד מול הייצור ומה
חוסם את מה:

- **חיוב** — אין סליקה ואין חשבונית. ממתין להחלטות של גל (ספק סליקה, ספק
  חשבוניות) ולא לקוד. שיעור המע"מ והמחירון נסגרו ב-29.7.
- **ממשק ניהול הזמנות פנימי (D4)** — לשונית "הזמנות" היא תצוגה, לא ניהול, ולכן
  ההודעות האוטומטיות ללקוחה על שינוי סטטוס **כובו** (ראו למטה).

## הערות מימוש

- DXF נכתב כ-R12 (AC1009) עם ישויות POLYLINE קלאסיות ולא LWPOLYLINE —
  LWPOLYLINE אינה חלק מ-R12; כך הקובץ נפתח בכל תוכנת CAD. מאומת בטסט
  round-trip מול ezdxf.
- פונט סטנסיל עברי חופשי לא נמצא (כצפוי בסעיף 15.5) — מומש מנגנון הגישור
  האוטומטי מהמפרט עם הפונט Secular One (עברית+לטינית, רישיון OFL): כל קונטור
  פנימי של אות מקבל גשרים אנכיים ברוחב minBridgeCut.
- מגבלת המכסה היומית נאכפת לפי ספירת גרסאות שנוצרו היום לפרופיל.
- תמיכה בשני ספקי LLM: OpenAI (ברירת מחדל כשמוגדר `OPENAI_KEY`, מודל
  `OPENAI_MODEL`, ברירת מחדל gpt-5.1) ו-Anthropic (`LLM_MODEL`). בחירה
  מפורשת דרך `LLM_PROVIDER` (repo variable: openai/anthropic).
- הדמיה תלת-ממדית: המשולשים מחולקים לפרוסות לאורך ציר הכיפוף לפני הכיפוף
  (אחרת הקשת נראית מצולעת), והמשטחים הכפופים מקבלים נורמלים רדיאליים
  אנליטיים לפני מתכת חלקים.
- **חלוקת העבודה בין ה-Worker לקופסה (27.7).** הצינור הכבד של היצירה — יצירת
  ההדמיות, חיתוכן לשורות, ההמרה לווקטור והעלאת הקבצים — רץ על שירות ה-vectorizer
  ב-Hetzner (`POST /api/generate`). ב-Worker נשארו התכנון (`planRender`), בניית
  הפרומפט, ולידציית הייצור ושמירת הגרסה. הסיבה: טבעת 53×10 מתוכננת לארבע הדמיות,
  וארבעה PNG של 1536x1024 שמפוענחים ונכתבים מחדש ב-JS — עם ארבעה payloads של
  debug — עוברים את מה ש-isolate של Cloudflare רשאי לצרוך, וההרצה נהרגה (1102 →
  503 ללא גוף) *אחרי* שהצינור הצליח, בלי שנשמרה גרסה. `src/lib/render/png.ts`
  נמחק: הוא נכתב מלכתחילה רק כי sharp לא רץ על Workers.
- **מפתחות.** הקופסה מקבלת `OPENAI_KEY` בלבד. את הקבצים היא כותבת ל-storage דרך
  כתובות העלאה חתומות שה-Worker מנפיק לה לכל נתיב — מפתח ה-service לא עוזב את
  ה-Worker, והשירות יכול לכתוב רק את הנתיבים שנחתמו.
- **תקרת ה-subrequests.** ל-Worker מותרות 50 בקשות חיצוניות ל-invocation. יומן
  הבק־אופיס חתם חמש כתובות לכל אחת מ-80 השורות (401 בקשות): החתימה ה-50 נכשלה,
  והשגיאה נבלעה — מהשורה העשירית ואילך היומן הופיע בלי תמונות ובלי הודעה. עכשיו
  הרשימה לא חותמת כלום ומחזירה קישור ל-`/api/debug/log/<id>/image/<name>`,
  שחותם על פי דרישה. כל בקשה חדשה = תקציב חדש.
- **למה ה-Worker נהרג באמצע יצירה (28.7).** לקוח קיבל `unknown · 503`, ו"נסה
  שוב" החזיר אותה מיד. נקבע מול `workersInvocationsAdaptive`: `exceededResources`
  — ותקרת ה-CPU נפסלה חד-משמעית (ההרוגים צרכו 20ms בחציון מול תקרה של 30 שניות,
  פחות מהרצות מוצלחות). כלומר **זיכרון**, 128MB, ובבקשה **יחידה** (שורת 04:00Z
  היא `reqs=1`). המקצה הגדול היה מסגור המועמדים, לא ה-payload של הקופסה: כל
  מועמד החזיק את `normalized`, וכל עוד הוא מוחזק הגאומטריה של כולם מוצמדת ואי
  אפשר לאסוף בין אחד לשני — 45.1MB בתום הקריאה מול 16.5MB אחרי התיקון. **זה לא
  נסגר**: רוב ההקצאה היא זבל בר-איסוף ואין מדידת זיכרון לכל בקשה, אז אי אפשר
  לקבוע שדווקא זה חצה את התקרה. האבחון המלא, מה שנפסל, והתיקון המבני אם זה
  חוזר — `docs/worker-memory-diagnosis.md`. הכלי:
  `.github/workflows/diagnose-worker.yml`.
- **הזמנה היא ישות (29.7, `0011_orders.sql`).** עד כאן כל הזמנה נדחסה
  ל-`inquiries` — טבלת טופס־צור־קשר — כשהמידות, המחיר ומזהה העיצוב יושבים
  ב-`message` כטקסט חופשי. `orders` מחזיקה את זה כשדות, עם צינור סטטוסים
  (`sent → approved → in_production → shipped`, `cancelled` בצד),
  `status_history` לכל מעבר, ו-`version_id` של **הגרסה שהוזמנה** — לא הנוכחית:
  אחרי ההזמנה אפשר להמשיך לערוך, ומה שנחתך חייב להיות מה שאושר. שורות
  היסטוריות לא הומרו — חילוץ מספרים מטקסט חופשי הוא ניחוש שנשמר כאילו הוא נתון.
- **למה אין מייל על שינוי סטטוס (29.7).** הוא נבנה, עבד, וכובה בהחלטת גל.
  הנימוק הוא סדר ולא ויתור: הודעה אוטומטית שווה בדיוק כמו הסטטוס שהיא מדווחת
  עליו, ואת הסטטוסים אי אפשר לתחזק ברצף מתוך תפריט על כרטיס ברשימה — לשם כך
  צריך ממשק ניהול הזמנות (`docs/TODO.md` D4). הודעה שמבטיחה "נכנס לייצור" על
  שדה שמתעדכן כשנזכרים גרועה משתיקה: היא הופכת מידע ישן להתחייבות. הנוסח
  (`orderStatusMail`) נשאר בקוד ובטסטים ואינו מחווט לשום מסלול, והמסך אומר
  במפורש שהלקוחה אינה מקבלת הודעה.
- **המחירים כוללים מע"מ (29.7).** קודם הם היו נטו ו-17% נוספו מעליהם, כך
  ש"מחיר הצמיד" היה מספר שאיש לא רואה, והמע"מ הופיע כשורת תוספת מעל הסה"כ.
  עכשיו המחירון הוא מה שמשלמים — צמיד סטנדרטי ₪399, טבעת ₪299, לפני משלוח
  וכולל אריזה — המשלוח מתווסף, והמע"מ (18%) נגזר מתוך הסכום (18/118) ומוצג
  כ"מזה". הרווח שבין 17% ל-18% נבלע בכוונה: המחירים לא הועלו כדי לפצות עליו.
- **המחיר מחושב בשרת.** `priceOf` ישב ב-`model.ts`, כלומר בדפדפן בלבד, והשרת
  קיבל `סה"כ` כמחרוזת בתוך גוף ההודעה. `src/lib/pricing.ts` הוא עכשיו המקום
  היחיד, שני הצדדים קוראים לו, וההזמנה נשמרת עם הפירוט שהשרת חישב. הטסטים
  נועלים את שני המספרים שנקבעו (399/299) ואת המבנה — שהסה"כ הוא בדיוק סכום
  הרכיבים ושהמע"מ אינו אחד מהם: מחיר שזז בשקט הוא מחיר שגובים אחרת ממה שהוצג.
- **מה נשלח למודל נשמר עכשיו (28.7).** היומן שמר את `prompt` — הטקסט של
  המשתמש — ולא את הפרומפט שנבנה ממנו ויצא ל-`gpt-image-1-mini`, וזו בדיוק
  השאלה הראשונה כשהדמיה חוזרת עבה מדי או עם מסגרת. `0009` הוסיפה
  `render_prompt`, `inputs` (מוצר, מידות, עובי, שורות/קריאות, פתח מינימלי,
  מפתח צבע) ו-`input_image_path` (קובץ ההשראה, שעד עכשיו נכנס לקריאה ונעלם).
  הפרומפט **לא** נכנס לרשימת היומן — ~3KB לשורה שאיש לא קורא — אלא נטען עם
  הפירוט, בחלון שנפתח בלחיצה. `insertRun` נופל חזרה לכתיבה בלי שלוש העמודות
  אם המיגרציה עוד לא רצה: שורה חסרה עדיפה על הרצה שלא נרשמה בכלל.
- **למה ה-SVG בבק־אופיס נראה שחור על אפור.** ה-SVG מהצינור נושא `fill="black"`
  על כל `path`, והכרטיס הגדיר `fill` על אלמנט ה-`<svg>` — ירושה, שמפסידה
  למאפיין מפורש על הצאצא. כלל CSS דווקא כן גובר על מאפיין תצוגה, ולכן הכלל
  מכוון עכשיו לצאצאים (`[&_svg_*]`). נוסף מתג רקע כהה/בהיר: מתכת בהירה על
  כמעט-שחור מראה את הצורה, ושחור על לבן מראה קווים דקים שנבלעים בזוהר.
- **קריאת היומן — מלכודת.** שורת `generation_runs` נכתבת באמצע הצינור, לפני
  שמירת הגרסה, כדי שגם דחיות יירשמו. `approved` אומר שהרנדר והווקטורייזר עברו,
  **לא** שנוצר עיצוב. הקריטריון להצלחה הוא קיום גרסה.
- **צינור אחד, קריאה אחת (30.7).** התכנון (`planRender`) גזר את מספר הפריטים
  בתמונה מהיחס — ואז הוסיף קריאה שנייה כדי להשלים ל"ארבע חלופות", כך שצמיד
  סטנדרטי (יחס 8.9 → שלוש שורות) שילם כפול עבור שישה מועמדים במקום שלושה.
  היעד בוטל: השורות נגזרות מהיחס, הקריאה אחת, ומספר החלופות הוא מה שיצא —
  פריט רחב מחזיר אחת. **גם עריכה** משתמשת באותו תכנון; קודם היא קיבלה שורה
  אחת בארבע קריאות בנימוק שמול רפרנס היחס מגיע מהרפרנס, נימוק שלא נמדד מעולם
  (45 ההרצות היו טקסט→תמונה). העלות ירדה פי 2–4, ל-~$0.006 להרצה.
  התקרה על מספר השורות אינה מספר שנבחר אלא **תקציב פיקסלים**: מתחת ל-3px
  הפתח המינימלי לא שורד את המעקב, ולכן היא נגזרת מ-`minHole` ומגובה הקנבס.
- **מעבדת הפרומפט החליפה את הצינור השני (30.7).** `/api/debug/run` היה מסלול
  יצירה נפרד — שורה אחת תמיד, ומעקב על התמונה כולה במקום חיתוך לפאנלים — ולכן
  מה שנבדק בו לא היה מה שרץ בייצור. הוא נמחק. במקומו `/api/generate` מקבל שני
  פרמטרים שמותרים לאדמין בלבד (`promptOverride`, `rowsOverride`), ומסך הכיול
  מריץ את **אותו** מסלול; העיצובים נשמרים תחת פרופיל `prompt-debug`. עם המחיקה
  נעלמו `generateRenderPng` ו-`vectorizeImageDebug`, וה-Worker הפסיק להחזיק
  לקוח של מודל תמונה — רק הקופסה מדברת עם OpenAI.
- **המסגור עבר ל-Worker שני (29.7).** `forme-frame` (`wrangler.frame.jsonc`,
  `workers/frame/`) ממסגר מועמד אחד ומחזיר רק את מה שהמסך צריך; `forme-studio`
  קורא אליו דרך service binding בשם `FRAME`, **סדרתית**, ונופל למסגור מקומי אם
  ה-binding חסר או נכשל. הסיבה היא מדידה בתיעוד ולא אינטואיציה: תקרת ה-128MB
  היא **לכל isolate ומשותפת לכל הבקשות שרצות בו**, ולכן subrequest ל-Worker
  *עצמו* — מה שנרשם קודם כחלופה — היה נותן round-trip ואפס תקציב. script אחר
  הוא isolate אחר. מנוע הגיאומטריה לא שוכפל: שני ה-Workers מייבאים את
  `src/lib/geometry/frameCutouts.ts`. **סדר הפריסה הוא תנאי** — `forme-frame`
  קודם, אחרת אין למה לקשור, ושניהם מאותו commit.
- **אפשר סוף-סוף למדוד זיכרון.** מ-30.6.2026 `workersInvocationsAdaptive`
  מחזיר `memoryUsageBytesP50..P999` (זיכרון ה-isolate מול 128MB).
  `diagnose-worker.yml` שואל אותם ומדפיס כמה MB נשארו לתקרה לכל script. זה
  הופך את "אנחנו מאמינים שזה זיכרון" למספר, וגם נותן מבחן לפני/אחרי: המרחק
  מהתקרה של `forme-studio` חייב לגדול אחרי הפריסה.
- **הבייסליין נמדד (28.7, 21:12Z).** `forme-studio` — worst P999 **135.4MB**
  מול תקרה של 128MB (‎-7.4MB headroom); `willit-web` באותו חשבון — 65.6MB.
  ושורות ההרוגים מתקנות את הסיפור: הן מראות זיכרון **נמוך** (48.4 / 69.6 /
  60.5MB), בעוד שמי שנגע ב-135.4MB הצליח. כלומר הבקשה שנהרגת היא **קורבן** של
  isolate שכבר חצה, לא בהכרח מי שמילא אותו — וזה מיישב את `reqs=1`: בקשה
  בודדת יכולה להיהרג בלי לחרוג בעצמה. מה שצריך לרדת הוא הזיכרון של ה-isolate
  המשותף. הפריסה של `forme-frame` (21:13Z) היא אחרי המדידה הזו, ולכן ההשוואה
  היא מול ההרצה המתוזמנת הבאה.
- **מנוע הגיאומטריה על הקופסה (29.7).** `geometry-service/` — שרת Node קטן
  שמייבא את `src/lib/geometry/frameCutouts.ts`, נארז ב-esbuild בזמן הפריסה ורץ
  בקונטיינר ליד ה-vectorizer (127.0.0.1:8100, חשוף תחת `/api/frame` על vhost
  הווקטורייזר, מאחורי אותו bearer). **חוקי הייצור לא שוכפלו** — מקור אחד, שני
  מקומות הרצה; מה שאסור הוא עותק שני בפייתון, לא הרצה של אותו קוד במקום אחר.
  בקופסה אין תקרת 128MB בכלל, ולכן זה עדיף על `forme-frame` שרק מעביר את
  המקצה ל-isolate אחר עם אותה תקרה. סדר הניסיונות: קופסה → `forme-frame` →
  מקומי, וכל יצירה כותבת שורה אחת עם המסלול שבו רצה. ה-vhost שייך
  ל-`deploy-vectorizer.yml` (פריסה אחת לא מוחקת את השנייה).
- **ומה שנמדד עד שזה נכנס (1.8).** בין הכתיבה למיזוג המרווח חזר מעצמו:
  `forme-studio` ירד מ-P999 של 136.4MB (‎-8.4MB מהתקרה, 12 הרוגים ב-30.7) ל-
  **70.3MB** (‎+57.7MB, 2 הרוגים) — כמעט בוודאות בזכות "צינור אחד" (`5610a62`),
  שהוריד את מספר המסגורים לבקשה. כלומר זה **לא** נכנס כתיקון לתקלה פעילה, וגם
  שתי ההרגות שנשארו הן לא זיכרון (35.1MB, 13ms CPU) — הן פתוחות בנפרד. מה
  שכן: 128MB לכל isolate הוא גג שלא ניתן להרים, חלון המדידה האחרון נגמר **לפני**
  שעבודת האותיות של 1.8 עלתה, ו-70MB הם 55% מהתקרה. זו הסרה של התקרה בזמן
  שהיא עולה קובץ אחד, לא כיבוי שריפה.
