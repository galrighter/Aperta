# אבחון מלא — האתר והעסק (14.8.2026)

> נבנה בריצת שבעה סוכני אבחון מקבילים. האבחון הקודם — `SITE_AUDIT_2026-08.md`
> — כיסה קוד/אתר ונסגר ברובו; המסמך הזה רחב ממנו: קוד, אבטחה, לוגיקה עסקית,
> UX/SEO, תפעול, שוק ותחרות, ומודל עסקי ותמחור. ממצאים שנסגרו שם אינם חוזרים.

## תקציר מנהלים — הסינתזה

**השורה התחתונה: ההנדסה בשלה בהרבה מהעסק.** ‏1,071 טסטים ירוקים, אפס ממצאי
אבטחה קריטיים/גבוהים, בשלות תפעולית יוצאת דופן למפעיל יחיד — ומנגד: אין
סליקה, אין אנליטיקס, אין נוכחות חברתית, אין הוכחה חברתית, אין זהות עסק
בתנאי השימוש, ומחיר שמעל עוגני השוק המקומיים לחומר חשוף. המוצר מוכן לקבל
לקוחות; העסק עוד לא מוכן להשיג אותם ולהרוויח מהם.

### ארבעת הממצאים הקריטיים (🔴)

> **סטטוס 15.8: כל ארבעת הממצאים הקריטיים נסגרו.** ‏4 נסגר במלואו — הנוסח,
> המבנה, והפרטים עצמם (רייטק, ע.מ 039569009, אזור התעשייה סלעית, טלפון).
> נותר מעבר עו"ד על הנוסח, כמו בשאר העמודים המשפטיים.

| # | ממצא | פרק | למה קריטי |
|---|------|-----|-----------|
| 1 | **באג שחזור מידות פעיל** — ‏resume של עיצוב שמור מנפח צמיד ב-~2 ס"מ וטבעת ב-~1.5 מידות בשקט (`design/page.tsx:987`); מייל ה-comeback שולח לקוחות היישר לתוכו | 3 | מייצר הזמנות במידה שגויה **עכשיו** — מוצר פיזי שלא ניתן להחזרה |
| 2 | **אפס גיבויים ל-Supabase** — אין pg_dump, אין PITR (תוכנית חינמית); כל המיגרציות רצות מחדש בכל פריסה בלי גיבוי מאחוריהן | 7 | תקלה אחת = אובדן צמית של כל העיצובים, ההזמנות והמשתמשים. נקודת האל-חזור היחידה בעסק |
| 3 | **עיצוב שהוזמן ניתן למחיקה** — ‏DELETE מוחק בקסקדה את גרסת הייצור (ה-SVG לחיתוך) של הזמנה פעילה | 3 | הזמנה משולמת בלי קובץ ייצור |
| 4 | **‏/terms בלי זהות עסק ובלי מדיניות ביטול** — נדרש בחוק הגנת הצרכן בעסקת מכר מרחוק | 4 | חשיפה משפטית + חסם האמון הזול ביותר לתיקון |

### מה חזק (לשמר)

- **קוד:** ‏typecheck/lint/build נקיים, ‏1,071 טסטים עוברים; מנוע הגיאומטריה
  והוולידציה מכוסים מצוין; כל ממצאי C1–C9 הקודמים אומתו כסגורים.
- **אבטחה:** אפס קריטי/גבוה; ‏IDOR סגור לרוחב, ‏RLS מלא, ‏SSRF ממותן, טוקני
  שיתוף חזקים. נותרו 5 הקשחות בינוניות/נמוכות.
- **תפעול:** ‏canary מקצה-לקצה, תורן אוטומטי, התראות מדורגות, ותרבות "כל
  תקלה הופכת לחיישן". עלות תשתית ‎~$20–40/חודש.
- **משפך:** מחיר גלוי משלב 1, שער חשבון בתזמון נכון, התאוששות ניתוק מלאה,
  תמחור שרת-בלבד idempotent.

### התמונה העסקית בשלוש שורות

1. **מיצוב:** אין מתחרה ישראלי שמציע עיצוב-מסיפור → ייצור מקומי בימים; אבל
   ‏₪399 לפליז חשוף מעל עוגני השוק (דנון 159–199 מצופה, ‏Onecklace כסף אמיתי
   ‏144–430, משלוח חינם נורמה). למכור "יחיד בעולם", לא "AI"; לטפל בהשחרה לפני
   שהיא מגיעה לביקורות.
2. **כלכלה:** תרומה ריאלית ‎+₪189 לצמיד (51%) — אבל רק עם ריכוז חיתוך
   לאצוות; פריט בודד הופך את התרומה לשלילית. תקרה נוכחית: ‏15–30 הזמנות/חודש
   (‏₪3–7 אלף) לצד פיתוח.
3. **צמיחה:** הדלי חורר — קודם סליקה+אנליטיקס+ניהול ציפיות, רק אז שיווק;
   הנכס השיווקי הוא סרטון התהליך (סיפור→לייזר→יד) וגלריה ציבורית שהתשתית
   שלה כבר קיימת.

### עשרת הצעדים הבאים (חוצה-תחומים, מהיר-ראשון)

> **סטטוס 15.8** — שבעה מהעשרה נסגרו בקוד. מה שנשאר פתוח (5, 8, וחלקי 9)
> אינו קוד אלא החלטות ונכסים של גל: חשבון סליקה, הצעות מחיר מספק, וצילומים.
> הפירוט בכל שורה.

1. ✅ **תיקון באג שחזור המידות** — `sizeOfDesign`/`sizeFieldsOfDesign` עוברים
   דרך `sizeFromBlank`; אותו תיקון ב-`syncFromAccount` ובשני מסכי האדמין.
2. ✅ **גיבוי DB יומי** — `.github/workflows/backup-db.yml` (‏pg_dump יומי,
   הצפנה, בדיקת dump ריק, התראת טלגרם על כשל) + `docs/RUNBOOK_RESTORE.md`.
   ‏`BACKUP_PASSPHRASE` הוגדר (15.8) — הגיבוי נשמר מוצפן.
3. ✅ **חסימת מחיקת עיצוב עם הזמנה פעילה** — `hasActiveOrderForDesign`,
   ‏409 עם סיבה שמוצגת.
4. ✅ **זהות עסק + מדיניות ביטול ב-/terms ובפוטר** — הנוסח, המבנה והפרטים.
   **נשאר לגל:** מעבר עו"ד על הנוסח.
5. ⬜ **סליקה בדף מתארח + חשבונית** — חוסם עסקי, לא טכני (TODO C1–C4).
6. ✅ **אנליטיקס + 5 אירועי משפך** — מיגרציה 0023, ‏`/api/events`,
   ‏`lib/client/track.ts`, וסיכום משפך בבק־אופיס. ביקון Cloudflare נדלק
   כשמוגדר `NEXT_PUBLIC_CF_BEACON_TOKEN`.
7. 🟨 **טלגרם כערוץ התראות שני** — נסגר: תקציב, רצף כשלים, הרצה תקועה,
   הזמנה חדשה, וכשל גיבוי. **נשאר:** uptime monitor חיצוני (UptimeRobot —
   הרשמה, לא קוד).
8. ⬜ **מחיר-אצווה לחיתוך שבועי** — ההחלטה הכלכלית החשובה ביותר. שיחות עם
   ספק.
9. 🟨 **ניהול ציפיות ואמון** — כפתור וואטסאפ חי בפוטר, ב-/contact ובצ'קאאוט
   (15.8). **נשאר:** צילומי אמת, כרטיס טיפוח, ‏featured→גלריה.
10. ✅ **הקשחות שרת להזמנות** — דחיית גרסת fail, הבחנת כשל שליפה מגרסה זרה,
    צילום מידות על ההזמנה (0022), ‏`paid_at` וסימון בבק־אופיס, נעילה
    אופטימית על מעבר סטטוס, תיחום PATCH, ‏`npm audit fix` (‏nanoid), ו-CSP
    מלא + כותרות אבטחה (אומת בדפדפן על 13 עמודים).

## מסגרת האבחון

| # | תחום | מה נבדק | סטטוס |
|---|------|---------|-------|
| 1 | קוד וארכיטקטורה | שערי איכות, ארכיטקטורה, חוב טכני, טסטים, CI/CD | ✅ הושלם |
| 2 | אבטחה | אימות, הרשאות, RLS, קלט, secrets, rate limiting | ✅ הושלם |
| 3 | לוגיקה עסקית | תמחור, הזמנות, מידות, ולידציית ייצור, מכסות | ✅ הושלם |
| 4 | UX / SEO / נגישות | אתר חי, משפך המרה, SEO, ביצועים, מובייל | ✅ הושלם |
| 5 | שוק ותחרות | מתחרים בישראל ובעולם, תפיסת פליז, מיצוב | ✅ הושלם |
| 6 | מודל עסקי ותמחור | unit economics, תקרת מודל, GTM, KPIs | ✅ הושלם |
| 7 | תפעול ושרידות | ניטור, נקודות כשל, גיבויים, עומס, עלויות | ✅ הושלם |

## נתוני רקע (נמדדו מהריפו, 14.8)

- **היקף קוד:** ‎~51K שורות אפליקציה — ‎`src/lib` 27.7K, ‏`src/components` 14.9K,
  ‏`src/app` 8.2K; ‏vectorizer (Python) ‎6.3K; ‏20 מיגרציות DB (‏806 שורות SQL).
- **היסטוריה:** 439 קומיטים מ-21.7.2026 (פרויקט בן פחות מחודש); מפתחים:
  galrighter + Claude.
- **CI/CD:** ‏12 workflows — ‏CI, ‏deploy (אתר/גיאומטריה/vectorizer), ‏מיגרציות,
  ‏canary, ‏ops-status, ‏sweep-jobs, ‏comeback, ‏notify-telegram, ‏diagnose-worker,
  ‏undici-override-check.
- **מסחר:** צמיד ₪399 / טבעת ₪299 כולל מע"מ, משלוח ₪35. **אין סליקה** —
  הזמנות מטופלות ידנית.

<!-- הפרקים מתמלאים עם סיום כל סוכן -->

---

# פרק 2 — אבטחה

## תקציר

**המצב טוב מאוד.** כל תשעת הממצאים הקריטיים/גבוהים מהאבחון הקודם (C1–C9) נסגרו
בפועל — כל אחד אומת מול הקוד החי, לא מול טבלת המצב. שרשרת הבודקים האנונימית
סגורה, הבעלות נאכפת בכל מסלול שמקבל מזהה, המכסה אטומית, שירותי הקופסה
fail-closed, וה-SSRF ממותן. **לא נמצאה אף פרצה קריטית או גבוהה חדשה.**

| חומרה | ספירה | פירוט |
|---|---|---|
| 🔴 קריטי | 0 | — |
| 🟠 גבוה | 0 | — |
| 🟡 בינוני/נמוך | 5 | nanoid (לא-נגיש), עוגיית אדמין גולמית, jobId עם design null, קלט ווקטורייזר לא-חסום, חוסר CSP |

## ממצאים פתוחים / חדשים

### 🟡 חדש — `nanoid` HIGH ב-npm audit (לא נגיש בפועל)

`npm audit` מחזיר כעת **2** פגיעויות במקום 1 שתועדה ב-O4. החדשה:
`nanoid <3.3.18` (High, GHSA-2v37-7h3g-55p8) דרך
`@tailwindcss/postcss → postcss → nanoid@3.3.16`.

- **ניצול:** לולאה אינסופית כש-`size=0`. transitive דרך `postcss` (כלי build),
  לא בזמן ריצה, ואין קלט תוקף שקובע `size` — **לא נגיש**, אבל High חדשה בעץ.
- **תיקון:** `npm audit fix` (לא-שובר, בניגוד ל-fast-xml-parser). לעדכן את O4
  מ"פגיעות אחת" ל"שתיים, שתיהן לא-נגישות".

### 🟡 פתוח (מתועד) — עוגיית האדמין היא הסוד עצמו, לא חתומה

`src/lib/admin.ts:50-53` — `requireAdmin` משווה את ערך העוגייה ישירות מול
`ADMIN_TOKEN` (`safeEqual`). אין HMAC, אין token פר-session, ‏Max-Age שבוע
(`session/route.ts:10,40`), ומינימום אורך 8 תווים בלבד (`admin.ts:11`).

- **מיתון שנוסף מאז:** ההתחברות מוגבלת — 10 ניסיונות ל-10 דקות לכל IP
  (`session/route.ts:32`), ולכן brute-force מקוון עוצר. מוריד מ-🟠 ל-🟡.
- **ניצול שנותר:** token שדלף פעם אחת (log, היסטוריית shell) תקף שבוע לכל
  מכשיר; אין revocation פר-session.
- **תיקון:** token אקראי ‎32B+‎, ולשקול עוגייה חתומה (nonce+HMAC) במקום הסוד
  הגולמי בדפדפן.

### 🟡 פתוח (מתועד) — `GET /api/generate/[jobId]` מדלג על בעלות כש-`design_id` null

`src/app/api/generate/[jobId]/route.ts:32-33` — ל-job שנכשל לפני קשירה לעיצוב,
מי שמנחש UUID מקבל את payload השגיאה. השפעה נמוכה (UUID-gated, מחרוזות שגיאה
בלבד). **תיקון:** ‏404 גנרי גם על job בלי `design_id`, או קשירת בעלות ל-account.

### 🟡 פתוח (מתועד) — קלט לא-חסום בשירות הווקטורייזר

`vectorizer/app/api/main.py:153` — ‏`prompt: str` בלי max; ‏`main.py:140` —
`base64: str` בלי cap. ממותן ע"י `client_max_body_size 25m` ב-nginx. בנוסף
`create_job` (‏`main.py:61`) חוסם את ה-event loop (‏`/api/generate` תוקן
ל-`anyio.to_thread`, ‏`/api/jobs` לא) — עניין זמינות.

### 🟡 הקשחה — אין CSP / security headers

אין `Content-Security-Policy`, ‏`X-Frame-Options`/`frame-ancestors`, ולא HSTS
בקוד (`next.config.ts` מגדיר רק `cache-control`; ‏`middleware.ts` רק
`X-Robots-Tag`).

- **ניצול:** ללא CSP כל XSS עתידי רץ בלי מגבלה; ללא `frame-ancestors` האתר
  ניתן למסגור (clickjacking על טופס ההזמנה).
- **תיקון:** ‏`headers()` ב-`next.config.ts` עם CSP הדוק (`default-src 'self'`
  ‏+ מקורות Supabase), ‏`X-Frame-Options: DENY`, ‏`Referrer-Policy`,
  ‏`X-Content-Type-Options: nosniff`.

### 🟢 ידוע ותקין — `fast-xml-parser` moderate

הפגיעות על `XMLBuilder`; הקוד מייבא `XMLParser` בלבד
(`src/lib/geometry/normalize.ts:1`), אין resolution של external entities (אין
XXE) ואין `DOCTYPE` בקונפיג. **לא נגיש**, כפי שתועד.

## נבדק ותקין

- **אימות זהות:** `readAccountId` משתמש ב-`getUser()` (אימות מול שרת, לא
  `getSession`), רענון עוגיות נכון (`account.ts:44-62`). מסלול POST שהנפיק
  זהות מטופס — נמחק.
- **בעלות (IDOR) — נסגר לרוחב:** `requireDesignAccess` נאכף בכל מסלול שמקבל
  מזהה עיצוב/גרסה — `designs/[id]` (GET/PATCH/DELETE), ‏`preview`,
  ‏`duplicate`, ‏`export`, ‏`vectorize`, ‏`shares`, ‏`generate`, וגם `orders`
  (`orders/route.ts:131`) ו-`feedback`. גרסאות מאומתות שייכות לעיצוב לפני
  שמירה.
- **שער האדמין:** כל `api/admin/*` מאחורי `requireAdmin`/`requireBearerAdmin`;
  התחברות rate-limited; השוואה קבועת-זמן; עוגייה
  `HttpOnly; Secure; SameSite=Lax`. ‏`llm-health`, ‏`debug/*`, ‏`jobs/sweep` —
  מאחורי שער.
- **שרשרת הבודקים (C1):** סגורה בשלוש הנקודות (`profiles/route.ts:14`,
  ‏`designAccess.ts:49-52`, ‏`designs/route.ts:63`).
- **RLS:** כל 11 הטבלאות עם RLS ואפס policies (deny-all ל-anon). ‏service key
  בשרת בלבד; הלקוח רק anon key ל-Auth.
- **ולידציית קלט:** zod עם תקרות גודל בכל מסלולי POST; ‏`/api/validate` ‏500KB
  ‏+ 60/דקה/IP; ‏`text-to-path` ‏60 תווים; תמונות 8MB עם allowlist media-type.
- **SSRF:** ‏`vectorizer/app/net.py` חוסם non-https, loopback, private,
  link-local, metadata; שני השירותים fail-closed (503 בלי token).
- **טוקן שיתוף:** base58 באורך 12 ≈ ‎2^70 מ-`crypto.getRandomValues`; ‏`GET
  /d/token` חושף רשימת שדות סגורה בלבד — לא SVG/פרומפט/מזהה.
- **Open redirect:** ‏`safeNext` חוסם `//`, ‏`/\`, וכל מה שלא מתחיל ב-`/`.
- **מיילים:** Resend REST ב-JSON — אין header injection; ‏path traversal
  בתמונות debug חסום ב-allowlist.
- **Rate limiting:** מפתוח לפי `cf-connecting-ip` שאינו ניתן לזיוף מאחורי
  Cloudflare.
- **Secrets:** אין `.env` ב-git; היחיד `NEXT_PUBLIC_*` הוא Supabase URL + anon
  key (ציבוריים בכוונה); אין דליפה ל-bundle לקוח.

**המלצה מיידית אחת:** `npm audit fix` (סוגר את nanoid). ההקשחה המשתלמת ביותר
אחריה — CSP + ‏`X-Frame-Options` ב-`next.config.ts`.

---

# פרק 5 — שוק ותחרות

## מתחרים ישירים בישראל

| מתחרה | הצעה | חומר | טווח מחירים (₪) | ערוצים |
|---|---|---|---|---|
| **Onecklace** | שרשראות/צמידי שם, חריטת כתב-יד, טביעת אצבע — Made in Israel | כסף 925, ציפוי זהב | 144–430; משלוח חינם מעל 250 | אתר, גוגל, מטא |
| **מגנוליה** | קולקציית התאמה אישית המונית — שם/תאריך/חריטה | מצופה | 69–279 | רשת חנויות + פרסום רחב |
| **שלומית אופיר** | תכשיטי מעצבת עם חריטה, עבודת יד ת"א | **פליז בציפוי זהב/רודיום** | צמיד חריטה ~140 במבצע | אתר, אינסטגרם, חנות |
| **Danon** | מותג ותיק (1975), עבודת יד | **פליז מצופה כסף/זהב 24K** | טבעות 179–279, צמידים ~159–199, משלוח חינם | רשת סניפים, אתר |
| **Pandora ישראל** | תכשיטי חריטה | כסף/מצופה | 285–379 | קניונים + אתר |
| **MOON / Talada / SOLIT / My Jewelry ועוד** | "תכשיטים עם משמעות" — שם, חריטה | כסף/מצופה | ~100–400 | פרפורמנס במטא |
| **סטודיואים לצורפות** (Hydra, MAI.DAN, UmiYo…) | עיצוב אישי מלא מול צורף | זהב, כסף | מאות–אלפים | אינסטגרם, פה-לאוזן |

הערות: mySoulmate ו-Ineri לא אותרו כאתרים פעילים. **מרמלדה מרקט סגור** —
ערוץ המרקטפלייס הישראלי לעבודת יד נעלם; נשארו מטא/אינסטגרם, גוגל ו-Etsy
(לייצוא).

## מתחרים גלובליים ב-AI Jewelry

- **Arcade** (arcade.ai, הושק 10/2024): פרומפט→מוצר דרך רשת אומנים; ‎$100 עד
  ‎$1,000+‎. המקבילה הקרובה ביותר — פעילה, אך יקרה ואיטית (שבועות).
- **Gemist**: התחילה B2C בהתאמה אישית — **פיבטה ל-SaaS B2B לצורפים** ($9M
  גיוס, ‎ARR ~$2.2M). איתות: B2C טהור של AI-custom התקשה כלכלית.
- **BLNG AI**: ‏Gen-AI ככלי למעצבים ומותגים, לא לצרכן.
- **מחוללי חינם** (Pixelcut, Fotor, OpenArt): הפכו "עיצוב תכשיט מפרומפט"
  לקומודיטי — הערך עבר לייצור בפועל ולחוויה.

## נתוני שוק

- שוק personalized jewelry גלובלי: ‎~$38.6B ב-2025, צמיחה ‎~8.7% לשנה.
- ‏Demi-fine: ‎$3.53B → ‎$5.74B עד 2031.
- ‏BoF-McKinsey ‏2026: תכשיטים — הקטגוריה הצומחת ביותר באופנה; עלייה
  ב-self-gifting.
- פרסונליזציה מוסיפה 20–40% ל-AOV ומורידה החזרות (8–15% מול 18–28%
  ב-fashion).

## תפיסת פליז — ומבחן ה-₪399

פליז מכיל נחושת → משחיר ועלול להשאיר סימן ירוק על עור מזיע. מותגי demi-fine
משתמשים בפליז **רק כבסיס תחת ציפוי** — כך שלומית אופיר ודנון בישראל.

**מבחן השוק:** ב-₪434 כולל משלוח, ‏Aperta יקרה מצמיד **כסף אמיתי** מותאם אישית
(Onecklace ‏144–430) ומצמיד פליז מצופה של מותג ותיק (דנון 159–199, משלוח
חינם). **המחיר לא עומד במבחן שוק על בסיס "חומר"** — רק על בסיס "אובייקט יחיד
בעולם שעיצבת בעצמך" (כמו ש-Arcade גובה ‎$100+‎). זה מחייב: (א) סטוריטלינג
one-of-one, (ב) פתרון גלוי להשחרה, (ג) ביטול תוספת המשלוח שהיא אנטי-נורמה
בקטגוריה.

## ערוצים ועלויות רכישה

בנצ'מרקים בקטגוריה: המרה 1.5–3%, ‏CAC ‏$50–90 (‎~₪180–330), נטישת עגלה
‎81.4%‎. מותגי קריאייטור/UGC מגיעים ל-CAC של ‎$10–30. משמעות: ב-CAC טיפוסי מול
סל ₪399 — פרסום ממומן קר בקושי מתכסה בהזמנה הראשונה; הנכס הוא סרטוני תהליך
(פרומפט→לייזר→כיפוף→יד) באינסטגרם/טיקטוק.

## פערי מיצוב

**הבידול:** אין בישראל שחקן שמציע עיצוב-עצמי מלא מסיפור → ייצור מקומי בלייזר
תוך ימים. המתחרים המקומיים — פרסונליזציה רדודה (שם/תאריך); הסטודיואים — עמוק
אבל יקר ואיטי. ‏Aperta בפער שביניהם: "עומק של סטודיו במחיר ומהירות של
e-commerce".

**במה הוא נשחק:** מחוללי AI חינמיים; ענקיות שמטמיעות AI-personalization;
הפליז שסותר את הפרימיום שהמחיר משדר; חסם כניסה נמוך (סטודיו לייזר + GPT);
ו"AI" כמסר שעלול להוזיל תפיסת ערך מול "אומן חתך ביד".

## 5 תובנות

1. **למכור "יחידוּת", לא "AI"** — הטכנולוגיה קומודיטי; הערך הוא "אין עוד אחד
   כזה בעולם".
2. **₪399 לפליז חשוף מעל תקרת השוק המקומית** — שריר רק כ"אובייקט מעוצב";
   תוספת ₪35 משלוח חריגה לרעה בקטגוריה שנורמת המשלוח בה חינם.
3. **בעיית הפליז היא בעיית ה-review הראשון** — השחרה תגיע לביקורות תוך 60–90
   יום; לטפל לפני סקייל.
4. **יחידת הכלכלה שברירית** — פרסום קר לא יעבוד לבד; תוכן תהליך = ‏CAC ברמת
   קריאייטור.
5. **הפרצדנטים מזהירים ומעודדים** — ‏Arcade מוכיחה ביקוש; ‏Gemist מזהירה
   שה-B2C שורד רק עם ייצור מהיר, זול ומקומי — בדיוק היתרון המבני של Aperta.

## 5 המלצות

1. **לגלם את המשלוח במחיר** (₪399 כולל הכול) ולהציג "משלוח חינם".
2. **לטפל בפליז עכשיו** — לכה אנטי-השחרה או ציפוי ב-upsell (‏₪+100), כרטיס
   טיפול, והפיכת החומר לנרטיב ("פליז מלא שמזדקן כמו דנים") במקום להסתירו.
3. **שיווק על סרטון התהליך** — ריל 20 שניות: סיפור → שרטוט → לייזר → כיפוף →
   יד. פוטנציאל CAC ‏₪40–100.
4. **מיצוב מול מתנות רגשיות** — "המתנה היחידה בעולם שמספרת את הסיפור שלכם";
   דפי נחיתה לאירועים (נישואין, לידה, גיוס/שחרור).
5. **לנצל את חלון הזמן** — חפיר לא-טכנולוגי: גלריית עיצובים/סיפורים ציבורית
   (SEO + הוכחה חברתית), אחריות החלפה, ואספקה בימים שאף מתחרה גלובלי לא משיג.

מקורות מלאים אצל סוכן המחקר; עיקריים: onecklace.co.il, danon-jewellery.co.il,
shlomitofir.co.il, techcrunch.com (Arcade), nationaljeweler.com (Gemist),
mordorintelligence.com, businessoffashion.com, branvas.com.

---

# פרק 4 — UX / SEO / ביצועים / נגישות

**היקף:** 13 העמודים הציבוריים באתר החי + קוד המשפך, הביצועים וה-i18n. ממצאים
שנסגרו ב-`SITE_AUDIT_2026-08.md` וב-`ACCESSIBILITY_PLAN.md` לא חוזרים כאן.

## תקציר

האתר במצב הנדסי ותוכני טוב מאוד: כל 13 העמודים נטענים, ‏titles/descriptions
ייחודיים, canonical לכל עמוד, sitemap/robots תקינים, המשפך בנוי בקפידה (מחיר
גלוי משלב 1, שער חשבון רק ברגע היצירה, שעון-אמת בהמתנה). **הפער המרכזי אינו
הנדסי אלא אמוני-מסחרי:** אין זהות עסק, אין טלפון/וואטסאפ, אין מדיניות ביטול
בתנאים, אין הוכחה חברתית, הגלריה מאוירת בלבד — ובקצה המשפך אין תשלום אלא
"נחזור אליך לתאם". עבור מותג לא-מוכר שמוכר ב-₪299–399 לקהל ישראלי במובייל,
אלה החסמים שקובעים את ההמרה.

**המשפך במספרים:** 6 מסכים מדף הבית עד הזמנה (5 במסלול הסיפור). מחיר מלא כבר
במסך 1; שער החשבון נפתח רק בלחיצת "צור"; ההמתנה עם שעון אמת, מצב ניתוק ומייל
השלמה.

## ממצאים

### 🔴 R1 — ‏/terms בלי זהות עסק ובלי מדיניות ביטול עסקה

באתר החי אין שם עוסק/ח.פ., כתובת או טלפון — רק `info@aperta-designs.com`; ואין
סעיף ביטול/החזרות כלל. בעסקת מכר מרחוק חוק הגנת הצרכן מחייב גילוי פרטי העוסק
ומדיניות הביטול לפני העסקה. המשפט היחיד ("ייצור לפי הזמנה — לא ניתן להחזרה…
למעט פגם ייצור") קבור במסך הסיכום בלבד (`src/i18n/he.ts:1561`). הפטור לטובין
בייצור אישי קיים בחוק — אבל לא פוטר מגילוי, ואינו חל על ביטול **לפני** תחילת
הייצור (שאפשרי כאן ממילא — הייצור מתחיל רק אחרי אישור שרטוט). גם ממצא אמון:
לקוחה שמוסרת כתובת וטלפון לא יודעת למי.

### 🟠 R2 — אין הוכחה חברתית ואין נכס ויזואלי אמיתי

אפס המלצות/ביקורות בכל האתר, ‏`instagram: null`
(‏`src/lib/site.config.ts:30`), והגלריה כולה איורים עם דיסקליימר "אינם מוצרים
ממשיים למכירה". למותג "תכשיט מ-AI" זה משאיר את שאלת הלקוחה המרכזית — "איך זה
נראה באמת?" — בלי תשובה. תשתית featured→גלריה כבר מתוכננת (`docs/SHARING.md`).

### 🟠 R3 — אין תשלום באתר; ההזמנה מסתיימת ב"נתאם תשלום"

`CheckoutScreen.tsx:3-6`, ‏`he.ts:1618-1620`. החלטה מודעת (TODO C), אבל
מבחינת המרה זה כפל-נטישה: חוסר ודאות בצ'קאאוט + שיחת מכירה שנייה שבה העסקה
יכולה למות.

### 🟠 R4 — ‏SEO: אין Organization/Product schema, ו-OG שגוי ברוב העמודים

structured data קיים רק כ-FAQPage (‏`faq/page.tsx:21-38`). אין Organization,
אין Product/Offer למרות מחיר קבוע. ‏`openGraph` מוגדר רק ב-root layout עם
`url: SITE.url` (‏`layout.tsx:31-38`) והעמודים דורסים רק title/description —
בכל העמודים חוץ מ-/story ה-og:url מצביע לדף הבית וה-og:title גנרי (אומת בחי).
שיתוף בוואטסאפ — הערוץ הישראלי המרכזי — מציג עמוד לא-נכון.

### 🟠 R5 — אין טלפון/וואטסאפ בשום נקודת מגע

‏/contact = טופס + מייל בלבד. בקהל ישראלי-מובייל, כפתור wa.me הוא ערוץ ההמרה
הזול ביותר — וערוץ ההרגעה הטבעי בצ'קאאוט ללא סליקה.

### 🟡 בינוני

- **R6** — ‏three.js נטען מיידית ב-/story במובייל אחרי קומיט #234 (ההדמיה
  במסך הראשון ⇒ ה-IntersectionObserver יורה בטעינה, במקביל ל-LCP). למדוד
  LCP/INP; אם נפגע — לדחות ל-idle.
- **R7** — משלוח ₪35 מתגלה רק במסך הסיכום; ההירו אומר "מ־₪299 כולל מע״מ".
  הפתעת-עלות קלאסית לנטישה.
- **R8** — נגישות, נשאר פתוח: שלושת מסכי סוף המשפך לא נסרקו ב-axe; בדיקת
  קורא-מסך אמיתי (החוסם המוצהר); חלופה טקסטואלית להדמיה; צ'קאאוט במקלדת;
  הכרעות שלב 0 (מורשה נגישות, ‏`phone: null` בהצהרה).
- **R9** — עברית בלבד בלי תשתית i18n (‏`he.ts` ‏1,952 שורות, ‏`lang="he"`
  קשיח). חוסם דוברי אנגלית בישראל וחו"ל; מנגד מכירה ל-EU תפעיל את ה-EAA.
  צריך החלטה מפורשת.
- **R10** — שני "דפי בית" מקבילים (`/` ו-`/story`) עם מסרים שונים, שניהם
  ב-sitemap. כשהניסוי יוכרע — לאחד או להפנות.

### 🟢 תקין — לשימור (נבדק)

משפך: מחיר בשלב 1, שער חשבון בתזמון נכון, פירוט מחיר בסיכום, שעון-אמת ומצב
ניתוק, שגיאות טופס ביציאה משדה + autocomplete. ביצועים: תמונות webp ≤96KB,
‏next/image עם priority, פונטים self-hosted, ‏three.js ב-dynamic import בכל 4
נקודות. ‏SEO בסיס: canonical, ‏sitemap עם lastmod, ‏robots, ‏308 www→apex,
‏llms.txt, ‏FAQPage. מובייל: viewport תקין, יעדי מגע ≥44px, ‏`touch-action`
נכון על קנבס ההדמיה.

## המלצות מתועדפות

| # | מה | מקור | אימפקט |
|---|-----|------|--------|
| 1 | סעיף "פרטי העסק" + "ביטול והחזרות" ב-/terms ובפוטר | R1 | **גבוה** — חסם האמון הזול ביותר; סוגר חשיפה משפטית |
| 2 | כפתור וואטסאפ בפוטר, ‏/contact, מסך "תודה" וצ'קאאוט | R5 | **גבוה** — מפצה מיידית על היעדר סליקה |
| 3 | הוכחה חברתית מינימלית: 3–5 תמונות אמת + featured→גלריה + 2–3 המלצות | R2 | **גבוה** |
| 4 | תיקון OG פר-עמוד + ‏Organization/Product JSON-LD | R4 | בינוני |
| 5 | דף תשלום מתארח (TODO C2) | R3 | בינוני-גבוה |
| 6 | "+ ₪35 משלוח" ליד המחיר, או הטמעה במחיר | R7 | נמוך-בינוני |
| 7 | מדידת LCP/INP ב-/story מובייל | R6 | נמוך-בינוני |
| 8 | סגירת שלב 4 בנגישות (קורא-מסך + סוף המשפך) | R8 | עקיף — הגנה משפטית |
| 9 | החלטה מתועדת על אנגלית | R9 | אסטרטגי |

---

# פרק 1 — קוד וארכיטקטורה

## תקציר

כל ארבעת שערי האיכות ירוקים: typecheck נקי, lint נקי, **1,071 טסטים ב-113
קבצים עוברים** (34.7 שנ'), ו-`next build` עובר. המערכת במצב הבריא ביותר שתועד
— כל ממצאי C1–C9 מהאבחון הקודם אומתו כסגורים בקוד. הסיכונים שנותרו הם
**תשתיתיים ולא קודיים**: קופסת Hetzner יחידה בלי בדיקת health ישירה, הרג
`exceededResources` בלתי-מוסבר שחוזר, ותלות ב-OpenAI גם במסלול הראשי וגם
ב-fallback. חוב מדיד: `design/page.tsx` צמח ל-1,988 שורות, ‏`lib/llm/pipeline.ts`
קוד מת, ‏bus factor אנושי = 1.

| שער | תוצאה |
|---|---|
| typecheck | ✅ 0 שגיאות |
| lint | ✅ 0 שגיאות, 0 אזהרות |
| test | ✅ 113 קבצים, 1,071 עוברים, 0 נכשלים |
| build | ✅ עובר |
| npm audit | ⚠️ 2 פגיעויות: nanoid (high, חדשה, dev-only) + fast-xml-parser (ידועה, לא נגישה) |

## ממצאים

### 🟠 גבוה

1. **קופסת Hetzner — נקודת כשל יחידה בלי בדיקת health ישירה.** כל היצירה תלויה
   בקופסה אחת; המיתון היחיד הוא canary כל שעתיים שרץ על GitHub schedule
   שנמדד בריפו כמאחר פי 5–10 — זמן גילוי אמיתי של קופסה מתה: 3–4 שעות.
   ‏`opsStatus` קורא רק את ה-DB, לא את הקופסה. אין קופסה שנייה ואין runbook.
2. **הרג `exceededResources` בחתימה "שקטה" — בלתי-מוסבר.**
   ‏memP99=35–70MB ו-cpuP99≤307ms — רחוק מכל תקרה — כולל אירוע של 52 בקשות,
   וחזר גם אחרי המעבר לקופסה. הניטור קיים (`diagnose-worker.yml`), אבל זו
   התקלה הפתוחה היחידה שמפילה לקוח בלי שורת יומן, ללא השערה נבדקת.
3. **תלות בספק יחיד — OpenAI בכל שלבי היצירה, כולל ה-fallback.** שלב הטקסט,
   מודל התמונה וה-fallback היקר — כולם OpenAI. מפתח Anthropic נפרס ל-Worker
   אבל משרת רק את `/api/llm-health` וקוד מת. נפילת OpenAI = אפס יצירות.
4. **bus factor אנושי = 1.** מפתח יחיד + Claude. המיתון — תיעוד יוצא דופן
   ותורן אוטומטי — חזק, אבל כל התשתית מוחזקת בראש אחד.

### 🟡 בינוני

5. **`design/page.tsx` — 1,988 שורות** (צמח ~30% מאז האבחון הקודם), ‏19
   ‏useEffects, אורקסטרציה (היסטוריה, OAuth stash, resume, heartbeat) בלי
   טסטים כי אין DOM תחת vitest. הקובץ המסוכן ביותר לשינוי בריפו. דפוס דומה
   ב-`RunsLog.tsx` (1,616 שורות).
6. **קוד מת:** ‏`src/lib/llm/pipeline.ts` + ‏`prompts.ts` — אפס קוראים;
   ‏`anthropic.ts`/`client.ts` נקראים רק מ-llm-health; ‏`ANTHROPIC_API_KEY`
   נדחף לפרודקשן בכל פריסה עבור קוד מת בעיקרו.
7. **nanoid high חדשה** — ‏dev-only, נסגרת ב-`npm audit fix` (ראו פרק 2).
8. **בדיקת undici החודשית מעולם לא רצה** — אותה מגבלת GitHub schedule.
9. **חורי כיסוי טסטים:** ‏36 route handlers, רק 2 עם טסטי route. ‏`orders`,
   ‏`shares`, ‏`export` — מסלולי הכסף — נבדקים רק דרך שכבת ה-lib. אפס טסטי
   DOM. המנוע הגאומטרי מכוסה מצוין (13 קבצי טסט) והווקטורייזר עם 11 קבצי
   pytest ב-CI.
10. **הזרקת JSON ב-heredoc של הסודות ב-`deploy.yml`** — סוד עם `"` או `\`
    שובר פריסה; ‏+ כתיב לא עקבי `DUTY_API_URL`/`DUTI_API_TOKEN`.
11. **`compatibility_date: "2025-05-01"`** — ‏~15 חודשים אחורה, בשני קובצי
    wrangler.
12. **`frameOne` (service binding) בלי timeout** — בניגוד ל-`frameOnBox`;
    חומרה ירדה (מסלול שני בסולם) אבל תלייה תתקע מועמד עד תקרת הבקשה.
13. **תלות ההתחברות ב-Resend** — קודי המייל של Supabase Auth עוברים דרך
    Resend; נפילה = כניסת מייל מתה, ואין ניטור.

### ✅ אומת כסגור (מול הקוד)

C2 במלואו (job על הקופסה, ‏replay, ‏sweep ב-Cron Trigger); ‏C5 (fail-closed +
‏SSRF guard); ‏C8 (שערי פריסה מלאים); ‏anyio.to_thread; ‏caps ליצירה; ‏C6/C7;
כפילות Worker↔שירותים פתורה נכון (`frameRequest.ts` אחד לשלושת המסלולים).

## המלצות

1. ניטור ישיר לקופסה מתוך `opsStatus()` או Cron Trigger של Cloudflare (פותר
   גם את איחורי ה-schedule של ה-canary).
2. ‏`npm audit fix`.
3. מחיקת `lib/llm/pipeline.ts`+`prompts.ts` והחלטה על עתיד מפתח Anthropic.
4. פירוק `design/page.tsx`: חילוץ שכבת ההיסטוריה ומסלול ה-resume/OAuth
   לקבצים נבדקים — החלקים עם הכי הרבה באגים היסטוריים והכי פחות כיסוי.
5. תיקון בלוק הסודות ב-`deploy.yml` (jq במקום heredoc) + ‏`DUTI`→`DUTY`.
6. טסטי route למסלולי הכסף (`/api/orders`, ‏`/api/shares/adopt`).
7. רענון `compatibility_date` בחלון שקט.
8. טווח ארוך: runbook שחזור קופסה + בחינת fallback ספק תמונה.

---

# פרק 7 — תפעול ושרידות

## תקציר

בשלות תפעולית יוצאת דופן למפעיל יחיד: canary אמיתי מקצה-לקצה, תורן אוטומטי
שמתקן ומאמת לבד, התראות עם דדופ, שרידות ניתוק ביצירה, ותרבות "כל תקלה הופכת
לחיישן". **אבל שני חורים מבטלים חלק ניכר מהביטחון:**

1. 🔴 **אין שום גיבוי ל-DB ול-storage.** ‏Supabase על התוכנית החינמית — אין
   גיבויים ואין PITR; אין `pg_dump` באף workflow. מחיקת/השחתת הפרויקט =
   אובדן מוחלט של עיצובים, גרסאות (כולל SVG לייצור), הזמנות ומשתמשים.
   מחריף: ‏`deploy.yml` מריץ את **כל** המיגרציות מחדש בכל פריסה בהסתמך על
   idempotency — כתיבה לא-אידמפוטנטית אחת רצה מול הייצור בלי גיבוי מאחוריה.
   נקודת האל-חזור היחידה במערכת.
2. 🔴 **כמעט כל ההתראות בערוץ אחד — Resend — שכבר נפל פעם (7.8).** התראת
   תקציב ומייל "הזמנה חדשה" הם מייל-בלבד; כשל Resend משתיק בו-זמנית את
   הלקוחות ואת ההתראות על עצמו. טלגרם כבר כתוב ומחווט — אבל רק לחלק.

## טבלת נקודות כשל

| רכיב | תרחיש | השפעה על לקוח | זמן זיהוי | מיטיגציה |
|---|---|---|---|---|
| Supabase (חינמי) | מחיקה/השחתה | **אובדן צמית של הכול** | מיידי | **אין. אפס גיבוי** |
| Supabase | השבתה זמנית | האתר כמעט כולו למטה | דקות–שעתיים | אין; טופס נופל ל-mailto |
| Hetzner (שרת יחיד) | נפילה | יצירה נכשלת; הרצה באוויר אובדת; צ'קאאוט על עיצוב קיים ממשיך | 15 דק'–שעתיים+ | ‏fallback מסגור בלבד |
| OpenAI | השבתה/quota (קרה 30.7) | אף יצירה לא עוברת — אין fallback ספק | דקות–שעתיים | טקסט בלבד נופל ל-Anthropic |
| Resend | מפתח פג (קרה 7.8) | הזמנות שקטות, קודי כניסה לא נשלחים, התראות מושתקות | **ימים** | טלגרם מחווט חלקית |
| Cloudflare | ‏exceededResources שקט | ‏503 בלי יומן | מסווג ב-diagnose | לא פתור |
| GitHub Actions | איחורי schedule (נמדד: 40–95 דק') | עקיף | — | ‏sweep עבר ל-Cron Trigger; ‏canary לא |
| גל (אדם יחיד) | חופשה/מילואים | הבטחת "10–14 ימי עבודה" בלי מנגנון גיבוי אנושי | — | התורן מתקן קוד, לא חותך פליז |

## ממצאים נוספים

- 🟠 **אין uptime monitoring חיצוני** — נפילה כוללת בלילה מזוהה רק ב-canary
  הבא (2–4 שעות). ‏UptimeRobot חינמי על `/` ועל `vec…/api/health` מוריד
  לדקות.
- 🟠 **restart של הקופסה מאבד הרצות באוויר** — כל פריסת vectorizer באמצע
  יצירה הורגת אותה (`docker rm -f`); הרצה עם תמונת השראה מוכרזת אבודה.
- 🟠 **אין rollback ואין בדיקת עשן אחרי deploy** — פריסה ששוברת ייצור חיה עד
  ה-canary הבא; דיספאץ' canary בסוף deploy.yml סוגר את רוב הפער.
- 🟠 **אין תקרת הוצאה גלובלית על LLM** — המכסה פר-פרופיל (50/יום); ‏500
  משתמשים בשעה = עד 25,000 יצירות מותרות ביום, וה-fallback היקר (פי 20–50)
  נדלק לכולם תחת עומס. ההגנה בפועל: hard limit בחשבון OpenAI בלבד.
- 🟡 התורן האוטומטי ממזג לייצור בלי עין אנושית — לגיטימי כעת; לקבוע מראש את
  הטריגר שמכבה זאת (הזמנה משולמת ראשונה?).
- 🟡 אין SLA-watchdog להזמנות ("הזמנה בת 3 ימים בסטטוס new").

## עלות תשתית חודשית (הערכה)

| רכיב | היום | בגדילה |
|---|---|---|
| Cloudflare Workers Paid | ~$5 | שטוח |
| Hetzner | ‎~€5–15 | מדרגה בשרת שני |
| Supabase | $0 | **$25 Pro — נדרש ממילא לגיבויים** |
| Resend | $0 | ‏$20 מעל ~3K מיילים |
| LLM APIs | ~$5–15 | היחיד שגדל ליניארית, מכפיל פי 20–50 ב-fallback |
| **סה"כ** | **~$20–40/חודש** | הסיכון אינו הקבועה אלא היעדר תקרה על המשתנה |

## המלצות (מכוילות לאדם יחיד)

1. **גיבוי DB יומי — השבוע.** ‏workflow עם `pg_dump` דרך `SUPABASE_DB_URL`
   (כבר קיים כ-secret) + ייצוא storage; או Supabase Pro ($25) לגיבויים+PITR.
   ההמלצה היחידה שאי-ביצועה יכול למחוק את העסק.
2. **טלגרם כערוץ שני לכל התראה קריטית — שינוי של שעה.** ‏`sendTelegram` כבר
   כתוב; להוסיף ל-quota, ‏unexpected-failure, ‏stalled, והזמנה חדשה.
3. **Uptime monitor חיצוני חינמי** עם התראה לטלגרם/SMS.
4. **canary אוטומטי בסוף כל deploy.**
5. **מגבלת קצב גלובלית ליצירה** + טלגרם ב-80%; לוודא hard limit נמוך
   ב-OpenAI.
6. **runbook שחזור של עמוד אחד** — לכתוב ביום רגוע; במילואים לא כותבים
   runbooks.
7. **watchdog הזמנות** — טלגרם על הזמנה מעל 24 שעות בסטטוס התחלתי.
8. **drain לפני פריסת vectorizer** כשיש הרצות באוויר.

**מה במפורש לא מומלץ** למבנה הנוכחי: HA/שרת שני, ‏Kubernetes, ספק תמונות שני,
מערכת ניטור עצמאית — משטח תחזוקה שאדם יחיד לא יתחזק; פתרונות הדקה-האחת למעלה
קונים את רוב הערך בכמעט אפס תחזוקה.

---

# פרק 3 — לוגיקה עסקית

## תקציר

הליבה בנויה טוב באופן יוצא דופן: המחיר מחושב בשרת בלבד (`priceFor`) עם בדיקת
התאמה מול המסך, המכסה היומית אטומית ב-DB (‏advisory lock), ההזמנה idempotent,
וצינור ההתאוששות מניתוק סוגר כמעט כל חור. אבל נמצאו **שני ממצאים חמורים בתפר
שבין מידות לשחזור עיצוב**, ושורה של ממצאי המשך סביב הזמנות.

## ממצאים

### 🔴 1. שחזור עיצוב שמור משחזר מידה שגויה — ניפוח שקט של ~2 ס"מ בצמיד / ~1.5 מידות בטבעת

`src/app/(site)/design/page.tsx:987`:
`const circP = Number(design.length_mm) + Number(design.gap_mm);`
(וגם `syncFromAccount` בשורה 349.)

זה היפוך שגוי של `computeSizing`: הפריסה היא ‎π·D_n − gapArc‎ כשה-D_n כולל
תוספת Fit (‏15 מ"מ ב-comfort), תוספת רוחב ואיבר ציר ניטרלי — וההמרה מיתר→קשת.
הפונקציה ההפוכה הנכונה **קיימת** (`sizeFromBlank`, ‏`src/lib/sizing.ts:138`)
ולא בשימוש.

**מספרים:** צמיד לפרק 165 מ"מ → פריסה 160.4; בשחזור: ‏160.4+25.4=186 →
יצירה מחדש מוסיפה שוב ease+allowance → פריסה ~182 (‏+21 מ"מ). טבעת US 7 →
‏+1.7 מידות.

**תרחיש:** לקוחה שיצירתה נקטעה מקבלת מייל comeback עם קישור resume, נוחתת
במסלול שממלא `circ` מהסכום השגוי, לוחצת "נסו שוב" — ‏`startGeneration` כותב
את האורך המנופח לעיצוב, והיא מזמינה צמיד לפרק 18.6 ס"מ כשמדדה 16.5. אין שום
חיווי. זה בדיוק מנגנון AP-0065 שהקוד מתעד כאסור (`fabrication.config.ts:109`),
חוזר דרך דלת אחרת — **ומייל ה-comeback שולח לקוחות היישר לתוכו.**

**תיקון (עדיפות 1):** להחליף את שורה 987 בקריאה ל-`sizeFromBlank`, ובטבעת
להמיר דרך `nearestUsRingSize`.

### 🔴 2. עיצוב נשאר בר-מחיקה אחרי הזמנה — מחיקה משמידה את הגרסה שהוזמנה

`src/app/api/designs/[id]/route.ts:68-85` (‏DELETE בלי בדיקת הזמנות תלויות);
‏`0001_init.sql:25` — ‏`design_versions … on delete cascade`; ההזמנה עצמה
‏`on delete set null`. שורד רק **הרישום**: ה-SVG של הגרסה שהוזמנה נמחק,
‏`orderedDesignInfo` מחזיר null, ואין דרך לייצא קובץ חיתוך.

**תרחיש:** לקוחה מזמינה, למחרת "מנקה" את החשבון; האדמין פותח את ההזמנה —
אין עיצוב, אין SVG, אין מה לחתוך. **תיקון (עדיפות 2):** לחסום DELETE כשקיימת
הזמנה שאינה cancelled (או soft-delete).

### 🟠 3. נתוני הייצור נקראים מהעיצוב ה**נוכחי**, לא מהגרסה שהוזמנה

`src/lib/orders/production.ts:47-57` — המידות נקראות מהשורה החיה למרות
שההערה מבטיחה "הגרסה שהוזמנה"; רק ה-SVG מהגרסה. ‏`PATCH /api/designs/[id]`
מקבל כל ערך חיובי בלי גבולות. תרחישים: שינוי מידה אחרי הזמנה במסלול Story
משנה את הוראות הערגול שהאדמין רואה; ‏thickness שגוי דרך PATCH מפיל את
‏`GET /api/orders/[id]` ב-500. **תיקון:** צילום מידות מלא על ההזמנה + תיחום
PATCH.

### 🟠 4. השרת מקבל הזמנה על גרסה שנכשלה בוולידציה

אין בדיקת `validation_status` ב-`/api/orders` — החסימה בדפדפן בלבד. הכשל
מתגלה רק ב-`export_blocked` אחרי שהלקוחה קיבלה אישור. **תיקון:** לדחות
גרסת fail בשרת.

### 🟠 5. ‏`version_id` שנופל בשקט → ייצוא גרסה שלא אושרה

`orders/route.ts:136-140` — ‏`getVersion(candidate).catch(() => null)`: כשל
DB חולף מאפס את הגרסה בשקט, והייצוא נופל ל-current — בלי אינדיקציה.
**תיקון:** הבחנה בין "גרסה זרה" לכשל שליפה + אזהרת אדמין על הזמנה בלי גרסה.

### 🟠 6. אין מצב "שולם" בצינור ההזמנות

הצינור: ‏sent→approved→in_production→shipped — בלי paid. הזמנה שנחתכה בלי
תשלום היא רק עניין של שכחה אנושית. גיבוי ה-mailto עוקף את המסד לגמרי.
**תיקון:** שדה `paid_at` + חיווי בתור על "אושר אך לא סומן תשלום".

### 🟡 ממצאי המשך

7. **`maxOpenAreaFailPct` מוצהר "תקרה קשיחה" אך לא נאכף** — הקונפיג
   (`fabrication.config.ts:84`) סותר את `validate.ts:231`; עיצוב 50%+ שטח
   פתוח יגיע לערגול ועלול להישבר שם.
8. **עדכון סטטוס הזמנה — read-modify-write בלי נעילה** — שני טאבים = אובדן
   רשומת מעבר + שני מיילים; וכל מעבר חוקי כולל shipped→sent. תיקון זול:
   ‏`eq("status", before.status)`.
9. **`/api/vectorize` בלי מכסה ובלי rate-limit** — הצפת אחסון/CPU אפשרית
   לבעלים מאומת. (עלויות LLM עצמן מגודרות היטב: ‏~$0.006–0.021 להרצה, תקרת
   50/יום/פרופיל.)
10. **מחירים קשיחים בקופי** (‏he.ts, ‏story.ts) מול `pricing.ts` — שינוי
    מחירון ישאיר את ההירו משקר. תיקון: ייבוא מ-pricing או טסט השוואה.
11. **Story — רוחב נגזר בלי צביטה יוצא מטווח הייצור** במחיר קבוע; טבעת רוחב
    25 מ"מ עוברת הכול ומתגלה כבלתי-ענידה אצל הלקוחה.
12. **עקיפות קלות:** מכסת הזמנות לפי מייל בלי נרמול case/נקודות; תקרת 500
    בתור האדמין תחסיר הזמנות בשקט כשהעסק יגדל.

### 🟢 נבדק ותקין

תמחור שרת-בלבד עם 409 על פער; מע"מ 18/118 נגזר; שני המסלולים באותה
`priceFor`. הזמנות idempotent דו-שכבתי. ‏`sizeIssue`/`assertBuildableDims`
שערים נכונים. ‏V4 החדשה סוגרת את פער #239/#235. מכסה אטומית עם advisory
lock. התאוששות ניתוק מלאה (replay, ‏claimRecovery, ‏rerun מוגבל-גיל).
מיילים עם בעלות יחידה על התראה ודדופ.

## סדר טיפול מומלץ

1. תיקון היפוך המידה ב-resume (ממצא 1) — **באג פעיל שמייצר הזמנות במידה
   שגויה, ומייל ה-comeback מגביר חשיפה.**
2. חסימת מחיקת עיצוב עם הזמנה פעילה (ממצא 2) — שורת בדיקה אחת.
3. צילום מידות על ההזמנה + תיחום PATCH (3) ודחיית גרסת fail בשרת (4).
4. הבחנת כשל שליפה + אזהרת אדמין (5).
5. ‏`paid_at` וחיווי תשלום (6).
6. השאר (7–12) — היגיינה.

---

# פרק 6 — מודל עסקי ותמחור

> הדוח המלא פורסם גם כדף מעוצב:
> https://claude.ai/code/artifact/b9f438d8-0f78-4d3d-a7b2-cb0a014d73d8
> סימון: **[עובדה]** — נמדד מהריפו/תיעוד; **[הערכה]** — אומדן/מקור חיצוני.

## תקציר

**המוצר מוכן יותר מהעסק.** הצינור הטכני סגור ברמה מרשימה; מה שחסר הוא בדיוק
מה שמכניס כסף: **אין סליקה** (TODO C — החלטות של גל, לא קוד), **אין
אנליטיקס** (אפס tracking בקוד — נבדק), **אין נוכחות חברתית**
(`instagram: null`), ואין הוכחה חברתית.

**כלכלת היחידה טובה — בתנאי אחד: batching.** בתרחיש ריאלי צמיד תורם
‎~₪190–240 להזמנה (51–65% מההכנסה נטו). התרחיש הפסימי שלילי, וכמעט כולו נובע
מגורם אחד: חיתוך לייזר של פריט בודד כעבודה נפרדת (דמי מינימום של מאות ₪)
במקום ריכוז שבועי לגיליון אחד. **קצב ייצור קבוע בסדרות הוא ההחלטה הכלכלית
החשובה ביותר של העסק.**

**עלות ה-AI זניחה** — ‎$0.006 להדמיה [עובדה, ‏`imagegen.ts`]; גם ביחס נדיב של
150 הדמיות פר הזמנה זה ‎~₪4.5. המגבלה היא זמן הידיים של גל.

## כלכלת יחידה — צמיד ₪399 + ₪35 (₪ להזמנה)

| רכיב | אופטימי | ריאלי | פסימי | הנחות |
|---|---|---|---|---|
| הכנסה נטו (אחרי מע"מ, ‏434÷1.18) | 368 | 368 | 368 | [עובדה] |
| פליז C260 כולל פחת קינון | ‎−3 | ‎−6 | ‎−12 | [הערכה] ‏$5.5–12/ק"ג |
| חיתוך לייזר (שירות חוץ) | ‎−15 | ‎−45 | ‎−180 | [הערכה] אצווה שבועית ↔ עבודה בודדת עם דמי מינימום |
| כיפוף, גדות, QA (שווי זמן) | ‎−25 | ‎−45 | ‎−80 | [הערכה] ‏20–60 דק' × ‏₪80/שעה |
| אריזה ומיתוג | ‎−8 | ‎−15 | ‎−25 | [הערכה] |
| משלוח בפועל (נגבים ₪35) | ‎−25 | ‎−35 | ‎−49 | [הערכה] ‏₪22–49 |
| LLM והדמיות פר הזמנה | ‎−1 | ‎−4 | ‎−12 | [הערכה] נדיבה |
| סליקה עתידית (‏1.4–3%) | ‎−6 | ‎−11 | ‎−15 | [הערכה] |
| פגומים/ייצור חוזר | 0 | ‎−18 | ‎−45 | [הערכה] ‏0/5/12% |
| **תרומה להזמנה** | **‎+285** | **‎+189** | **‎−50** | |
| **% מההכנסה נטו** | **77%** | **51%** | **שלילי** | |

**טבעת ₪299:** הכנסה נטו ₪283; החיתוך עולה כמעט אותו דבר (setup, לא שטח)
והכיפוף קשה יותר. תרומה ריאלית **‎₪120–150 (‏45–50%)** — הטבעת פחות רווחית
מהצמיד. לא להוזיל אותה בקמפיינים; סט הוא הדרך למכור אותה.

**שורת החיתוך היא כל הסיפור** — הפער בין ‎−15 ל-‎−180 הוא ההבדל בין עסק
לתחביב. ריכוז שבועי לגיליון אחד מזיז מהעמודה הפסימית לריאלית, במחיר SLA של
עד ~10 ימי עסקים.

## תקרת המודל — מפעיל יחיד

‎60–100 דקות ידיים נטו להזמנה [הערכה] ⇒ **‎15–30 הזמנות/חודש** לצד פיתוח
(תרומה ‏₪3–7 אלף/חודש — עסק צד), ‏~80–120 במשרה מלאה עם batching. צווארי
בקבוק לפי סדר: לוגיסטיקת חיתוך → כיפוף/גימור → שירות לקוחות → גבייה ידנית
(נבחר מרצון, קל להסיר). המשמעות: להעלות תרומה להזמנה (tiering, סטים, B2B) או
להוריד דקות-ידיים (batching, פוליש מגנטי — נרכש ועובד מ-17.8, תליונים בלי
כיפוף) — עדיף שניהם.

## תמחור

הלקח מהמעבר למחיר קבוע [עובדה, הערות `pricing.ts`]: **מחיר חייב להיות ידוע
לפני היצירה** — עיקרון שלא סותר tiering, רק תמחור לפי פרמטרים שהמערכת קובעת.

- **Tiering לפי גימור** — פליז ₪399 / ‏e-coat ‏+₪80–100 / כסף-זהב ‏+₪150–250.
  עומד בעיקרון (הלקוחה בוחרת לפני), ופותר גם את ההשחרה.
- **הנחת סט** צמיד+טבעת ₪649 — כמעט חינם בקוד, מעלה סל, נחתכים מאותו גיליון.
  מומלץ מיד.
- **Fast track** — מתנגש עם ה-batching; לא עכשיו.
- **משלוח ₪35** — ‏pass-through מול העלות בפועל; תקין. משלוח חינם מעל סט.

## סיכונים על התפר הדמיה↔מציאות

1. **פער ציפיות** — ההדמיה (metalness 0.95, גוון מוזהב) מול פליז חתוך אמיתי.
   טיפול: צילומי אמת בעמוד המוצר ובמסך הסיכום.
2. **השחרה** — כימיה, לא פגם, אבל לקוחה שלא הוזהרה תחווה פגם. שלוש שכבות:
   כרטיס טיפוח, פסקה ב-FAQ, ‏e-coat בתשלום. ‏C260 דל-עופרת + ‏EN 1811 כבר
   מתועדים [עובדה] — נכס שיווקי ("בטוח לעור") שלא מסופר.
3. **אפס הוכחה חברתית** — כל הזמנה בחודשים הקרובים שווה יותר כתוכן וכביקורת
   מאשר כהכנסה.
4. **מדיניות ביטולים (C6)** — לנסח במדויק לפני שמחברים סליקה; אחרי כסף
   אמיתי, מחלוקת ביטול היא chargeback.

## ‏Go-to-Market — חודשיים קדימה

**העיקרון: קודם לאטום את הדלי, אחר כך לשפוך מים.**

1. **שבועות 1–2 — כסף:** סליקה בדף מתארח (בנפח הנוכחי: Meshulam/Grow או
   PayPlus [הערכה]) + חשבונית (חשבונית ירוקה/iCount); לסגור את ניסוח C6.
2. **שבוע 2 — מדידה:** ‏Cloudflare Web Analytics (חינם, בלי באנר) או
   Plausible + חמישה אירועי משפך. שעות עבודה.
3. **שבועות 2–3 — ניהול ציפיות:** צילומי אמת, כרטיס טיפוח, "פליז חי ומשתנה"
   בעמוד המוצר. לפני כל שיווק.
4. **שבועות 3–8 — נוכחות:** אינסטגרם בפורמט "היא כתבה משפט, זה מה שנחתך";
   גלריה ציבורית מ-featured עם "להזמין כזה".
5. **מחודש 2 —** מייל בקשת ביקורת שבועיים אחרי משלוח; ניסוי מדיה בתשלום רק
   אחרי 10+ הזמנות בכרטיס וגלריה חיה.

**מה לא עכשיו:** קמפיינים בתשלום, ‏subscription, אנגלית/חו"ל, מוצרים חדשים —
לפני שהמשפך מדוד ומשלם.

## תעדוף הרחבות (BUSINESS_EXPANSION 1–9 מבין הייצור/ההיצע)

**המספרים עודכנו ב-17.8** אחרי שהפוליש המגנטי (שהיה ‎#3) נרכש, עובד, וירד
מהרשימה — הרשימה מוספרה מחדש, והשורה שדירגה אותו נמחקה מכאן. הדירוג עצמו לא
נגזר מחדש.

| דירוג | רעיון | אימפקט/מאמץ | שורה תחתונה |
|---|---|---|---|
| 1 | ‏#6 מידות ילדים | גבוה/נמוך | בעיקר קונפיג טווחים; שוק מתנות. **ראשון** |
| 2 | ‏#1 ציפוי e-coat | גבוה/בינוני | מנטרל השחרה + ‏tier פרימיום. פיילוט מיד |
| 3 | ‏#7 תליונים | גבוה/בינוני | חיתוך בלי כיפוף — תוקף את צוואר הבקבוק; מוצר כניסה ₪149–199 |
| 4 | ‏#5 ציפוי זהב/כסף | בינוני/בינוני | ה-tier היקר, אחרי e-coat |
| 5 | ‏#2 ‏e-coat גוונים | בינוני/בינוני | הרחבה של #1 אחרי שהוכח |
| 6 | ‏#3 עוביים | נמוך/נמוך | מלאי+כיול; לדחות עד ביקוש |
| 7 | ‏#4 חומרים נוספים | בינוני/גבוה | כיול מחדש; נירוסטה קשה לכיפוף. מאוחר |
| 8 | ‏#8 סטודיו עריכה מלא | נמוך/גבוה | לא מזיז הכנסה עכשיו |
| 9 | ‏#9 מעצב חיצוני | נמוך/בינוני | קונסיירז' — רק בנפח גבוה |

**תוספות:** ‏B2B מתנות/אירועים (הזמנה אחת = גיליון מלא — פותר את הכלכלה
מהשורש); גלריה ציבורית כמנוע ויראלי; קולקציות קפסולה עם יוצרות; כרטיסי מתנה
(מקדים תזרים, בלי ייצור); קמפיינים סביב תאריכים.

## ‏KPIs

שכבת הנתונים התחתונה מצוינת [עובדה] — ‏designs/versions/orders/
generation_runs/shares ניתנים לשליפה ב-SQL היום. חסר לגמרי: ראש המשפך
(ביקורים, מקורות, נטישה לפני עיצוב ראשון) וביקורי `/d/` והמרות "להזמין כזה".
הנדרש: פיקסל אנליטיקס + 5 אירועים (צפייה, התחלת עיצוב, גרסה ראשונה,
צ'קאאוט, הזמנה) + עמוד סיכום שבועי בבק-אופיס. ימים בודדים.

## עשר המלצות — מהיר-ראשון

1. סליקה בדף מתארח + חשבונית (C1–C4 + ניסוח C6). חוסם את כל השאר.
2. אנליטיקס + חמישה אירועי משפך. שעות.
3. קצב חיתוך שבועי ומחיר-אצווה עם ספק (3 הצעות מחיר). מזיז את הכלכלה
   מפסימי לריאלי.
4. ניהול ציפיות: צילומי אמת + כרטיס טיפוח. לפני כל שיווק.
5. הנחת סט ₪649 + משלוח חינם לסט.
6. אינסטגרם בפורמט "משפט → תכשיט".
7. מידות ילדים (#7).
8. גלריה ציבורית מ-featured + מדידת /d.
9. פיילוט e-coat + תמחור מדורג לפי גימור.
10. תליונים כמוצר כניסה + מסלול B2B מתנות.

**הנחות מוצהרות:** הייצור בשירות חוץ; מחירי חיתוך/משלוח/פגומים — טווחי שוק,
לא חשבוניות; יחס יצירות→הזמנה נדיב בכוונה; גל ממשיך כמפעיל יחיד.
