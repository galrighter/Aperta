# TODO — מה שנשאר פתוח

**סטטוס: דחוי.** כרגע העבודה מתמקדת בעיצוב, בסטודיו ובבקאנד.

| | סעיף | |
|---|---|---|
| **G** | להריץ את `0004_accounts.sql` | חוסם את רישום המשתמשים ואת המספר הסידורי |
| **E** | חשיפת `/debug` לציבור | טקסט של לקוחות נגיש בלי אימות |
| **A–D** | הזמנה וחיוב | המסלול מחזיר 500; אין סליקה, חשבונית או התראה |
| **F** | כותרת עברית | ה-`<title>` היחיד של האתר הוא הטאגליין האנגלי |

---

# G. להריץ את `0004_accounts.sql` — חוסם את הרישום

רישום המשתמשים והמספר הסידורי כתובים ומוכנים, אבל שניהם נשענים על עמודות
שהמיגרציה מוסיפה: `profiles.kind/email/phone` ו-`designs.serial`.

**סדר הפעולות: קודם המיגרציה, אחר כך ה-merge.** המיגרציה מוסיפה עמודות בלבד
ולכן היא בטוחה מול הקוד שרץ בייצור עכשיו; הכיוון ההפוך — קוד חדש מול מסד ישן —
משאיר את `/design` בלי מסלול יצירה עד שהיא תרוץ.

- [ ] **G1. להריץ את `supabase/migrations/0004_accounts.sql` בייצור.** *(גל בלבד)*
      אותן שתי דרכים כמו ב-A1: secret `SUPABASE_DB_URL` של ה-session pooler +
      הרצת ה-workflow, או הדבקה ב-SQL Editor. שימו לב ש-`0002` ו-`0003` עדיין
      לא רצו — אם מריצים ב-SQL Editor, להריץ אותן לפי הסדר.
- [ ] **G2. לוודא אחרי ההרצה:** `/design` פותח את שער הזיהוי בלחיצה על "יצירה",
      עיצוב חדש מקבל `RM-0001` ואילך, ו-`/admin` → לשונית "עיצובים" מציגה אותו.

מה קורה בלי המיגרציה: הסטודיו הפנימי (`/studio`) ממשיך לעבוד — יש לו נתיב
תאימות מפורש — וההרשמה מחזירה `503 schema_outdated` עם הודעה שאומרת בדיוק זאת,
במקום 500 סתום. זה מכוון: הכשל הקודם באותו מקום היה שקט (ראו את פתיח הסעיף הבא).

- [ ] **G3. שדרוג הזיהוי לקוד חד־פעמי במייל** — תלוי ב-**A3** (ספק דואר).
      היום אין סיסמה ואין אימות: מי שמזין מייל קיים נכנס לחשבון הקיים. זה מספיק
      לבטא סגורה עם חברים, ולא מספיק ברגע שבחשבון יושבות הזמנות אמיתיות.

> **תלוי בו: A2.** לשונית "עיצובים" בבק־אופיס יושבת מאחורי `requireAdmin`, ולכן
> בלי `ADMIN_TOKEN` היא מחזירה `503 admin_disabled` גם אחרי G1.

---

# הזמנה וחיוב

כדי שלקוחה תוכל להשלים הזמנה ולשלם.

## המצב היום (נמדד מול הייצור, 2026-07-26)

מסלול ההזמנה **לא עובד בכלל**. שלוש תקלות בלתי תלויות על אותו מסלול:

```
POST https://rmjewel.com/api/inquiries  → 500
  "Could not find the table 'public.inquiries' in the schema cache"
GET  https://rmjewel.com/api/inquiries  → 503  admin_disabled
GET  https://rmjewel.com/api/profiles   → 200  (המסד עצמו תקין)
```

הסיבה: `.github/workflows/migrate.yml` דורש secret בשם `SUPABASE_DB_URL`, וכשהוא
חסר ה-workflow יוצא ב-`exit 0` עם warning בלבד. `0001_init.sql` הורצה בזמנו ידנית,
`0002_site.sql` — שיוצרת את `inquiries` — לא. לכן `profiles` ו-`designs` קיימות
ו-`inquiries` לא.

מה שנפגע: הצ'קאאוט בסוף משפך `/design` (`submitOrder`). עמוד `/order` הוסר מהאתר
כי הטופס שלו מעולם לא עבד.

---

## A. להחיות את המסלול הקיים — חוסם

- [ ] **A1. להריץ את `supabase/migrations/0002_site.sql` בייצור.** *(גל בלבד)*
      להוסיף secret `SUPABASE_DB_URL` — חובה מחרוזת של ה-**session pooler**
      (`*.pooler.supabase.com`, פורט 5432, IPv4). החיבור הישיר
      `db.<ref>.supabase.co` הוא IPv6-only ו-runners של GitHub Actions נכשלים עליו.
      תווים מיוחדים בסיסמה — percent-encoding. ואז `workflow_dispatch` על
      "Apply Supabase migrations". חלופה: להדביק את הקובץ ב-SQL Editor.
- [ ] **A2. להגדיר `ADMIN_TOKEN`.** בלעדיו `/admin` חסום ו-`GET /api/inquiries`
      מחזיר `503 admin_disabled` — כלומר גם אחרי A1, פנייה שנשמרת לא נראית לאיש.
      מאז לשונית "עיצובים" זה חוסם גם את הצפייה במה שחברים עיצבו (G).
- [ ] **A3. התראה על פנייה חדשה.** אין בריפו שום קוד שליחת מייל — לא Resend, לא
      nodemailer, לא SMTP. להוסיף שליחה ל-`info@rmjewel.com` ב-`createInquiry`.
      דורש ספק + `RESEND_API_KEY` (או מקביל) כ-secret של ה-Worker, ואימות הדומיין
      `rmjewel.com` אצל הספק.
- [ ] **A4. מייל אישור ללקוחה** — מספר הזמנה, סיכום הפריט והמחיר. היום היא רואה
      מספר הזמנה על המסך בלבד; אם היא סוגרת את הטאב, אין לה שום אסמכתה.
- [ ] **A5. גיבוי כשה-API נכשל.** `submitOrder` מציג `checkoutError` וההזמנה אובדת.
      ליפול ל-`mailto:` עם כל הפרטים, כמו ב-`/contact`, כדי שלא תיעלם אף הזמנה.

## B. תוכן ההזמנה — מה שנשלח היום חסר

- [ ] **B1. השרטוט לא נשלח.** `submitOrder` שולח `מזהה עיצוב: <uuid>` בלבד. לצרף
      את ה-SVG/DXF של הגרסה הפעילה, או קישור חתום אליה — בלי זה אי אפשר לייצר
      מההזמנה בלי לחפש ידנית במסד.
- [ ] **B2. פירוט מחיר.** נשלח `סה"כ` בלבד, בזמן ש-`priceOf()` כבר מחשב
      `base / widthAdd / complexity / packaging / shipping / tax`. לשלוח את הפירוט
      המלא — הוא נדרש גם לחשבונית.
- [ ] **B3. הזמנה היא לא "פנייה".** הכל נדחס היום ל-`inquiries`, טבלת טופס־צור־קשר,
      כשדה `message` של טקסט חופשי. הזמנה צריכה ישות משלה עם סטטוסים
      (`נשלחה → אושרה → בייצור → נשלחה ללקוחה`), סכום, וקישור ל-`designs`.

## C. חיוב

- [ ] **C1. לבחור ספק סליקה.** ישראלי (Tranzila / Cardcom / PayPlus / Meshulam) או
      Stripe. משפיע על כל היתר בסעיף הזה.
- [ ] **C2. דף תשלום מתארח (hosted).** `CheckoutScreen` לא אוסף פרטי כרטיס — החלטה
      מודעת ומתועדת בקוד, כי איסוף מספרי כרטיס באתר בלי ספק מחובר הוא חשיפה
      אמיתית. המעבר צריך להיות לדף של הספק, לא לשדות כרטיס משלנו.
- [ ] **C3. Webhook לאישור תשלום** → עדכון סטטוס ההזמנה (תלוי ב-B3).
- [ ] **C4. חשבונית/קבלה.** חובה חוקית. ספק חשבוניות (Morning / iCount / חשבונית ירוקה)
      או הנפקה ידנית — צריך להחליט.
- [ ] **C5. שיעור המע"מ.** `model.ts` מחשב `* 0.17`. מע"מ בישראל עלה ל-18% ב-2025 —
      **לאמת מול רואה החשבון ולעדכן**; אם 18% הוא הנכון, כל מחיר שמוצג באתר היום
      נמוך מדי.
- [ ] **C6. מדיניות ביטול והחזרה.** `disclaimersTitle` כבר אומר "ייצור לפי הזמנה —
      לא ניתן להחזרה". לוודא שהניסוח עומד בחוק הגנת הצרכן לגבי מוצר מותאם אישית,
      ושהוא עקבי בין `/terms` לבין מסך הסיכום.

## D. תפעול אחרי ההזמנה

- [ ] **D1. מסך אדמין להזמנות.** `AdminDashboard` מציג `inquiries` בלבד.
- [ ] **D2. מעקב סטטוס ללקוחה** — לפחות מייל בכל שינוי סטטוס.
- [ ] **D3. ייצוא לייצור** מההזמנה המאושרת ישירות ל-DXF.

---

## סדר מומלץ

`A1 → A2 → A3 → A5` נותן מסלול הזמנה שעובד ומגיע לבן אדם, בלי סליקה ובלי שינוי
סכימה. זה החסם האמיתי. `B` ו-`C` הם המוצר המלא ואפשר להריץ אותם אחריו.

---

# E. לסגור את `/debug` — חשיפת מידע לקוחות

**זה הפריט הדחוף ביותר במסמך.** נמדד מול הייצור, בלי שום אימות:

```
GET /api/debug/log                       → 200, 80 הרצות
GET /api/debug/log/<id>                  → 200, 288KB פירוט מלא
GET /api/debug/log/<id>/image/render     → 200, PNG של 1.7MB
```

מתוך 80 ההרצות, **15 מכילות טקסט חופשי שלקוחות כתבו** — למשל
`"תיאור הלקוחה: עלים המחוברים אחד לשני, רק קווי המתאר שלהם..."`. נחשפים גם
הפרומפט המלא, ההדמיות שנוצרו, זמני ריצה, שם המודל ומדדים פנימיים.

`requireAdmin` כבר קיים ב-`src/lib/admin.ts` ומשמש ב-`inquiries` — אבל **בכל
`src/app/api/` הוא מופיע רק שם**. ארבעת מסלולי ה-debug ועמוד `/debug` לא בודקים
כלום.

- [ ] **E1. `requireAdmin(req)` על ארבעת המסלולים:**
      `api/debug/log/route.ts` · `api/debug/log/[id]/route.ts` ·
      `api/debug/log/[id]/image/[name]/route.ts` · `api/debug/run/route.ts`
- [ ] **E2. שער על `src/app/debug/page.tsx`** — אותו מנגנון של `/admin`
      (עוגיית `forme_admin` דרך `POST /api/admin/session`).
- [ ] **E3. `Disallow: /debug` ב-`src/app/robots.ts`.** היום נחסמים `/api/`
      ו-`/admin` בלבד. זו הקשחה משנית — robots הוא בקשה, לא הגנה; E1 הוא ההגנה.
- [ ] **E4. לוודא שאין מסלול נוסף בלי שער** — `grep -rL requireAdmin` על
      `src/app/api/` אחרי התיקון.

> **תלוי ב-A2.** `requireAdmin` מחזיר `503 admin_disabled` כל עוד `ADMIN_TOKEN`
> לא מוגדר. כלומר E1 יסגור את `/debug` גם בפניך עד שתגדיר את הטוקן — לכן
> **A2 לפני E1**, אחרת תאבד את היומן בזמן הכיול.

---

# F. כותרת עברית — אין מילה בעברית ב-`<title>`

`he.site.tagline` הוא `"Precision jewelry, formed around you."`, והוא הכותרת
**היחידה** של אתר `lang="he" dir="rtl"`:

| מקום | קובץ |
|---|---|
| `<title>` של השורש | `src/app/layout.tsx` |
| `<title>` של קבוצת האתר | `src/app/(site)/layout.tsx` |
| `og:title` + `twitter:title` | `src/app/layout.tsx` |
| `name` של ה-manifest | `src/app/manifest.ts` |
| הטקסט מתחת ללוגו בפוטר | `src/components/site/SiteFooter.tsx` |

התוצאה: בתוצאת חיפוש עברית, ובשיתוף בוואטסאפ, אין מילה אחת בעברית.

- [ ] **F1. להוסיף מחרוזת כותרת בעברית** ולהשתמש בה ב-`title`/`og:title`/
      `twitter:title`/`manifest.name` — **בלי לגעת ב-`tagline` עצמו**, שנשאר
      הטאגליין של המותג בפוטר. למשל `RM JEWEL — תכשיטים בעיצוב אישי`.
- [ ] **F2. להחליט אם `brandHe` ("אר. אם. ג'ואל") נכנס לכותרת** או נשאר בצד.

זו החלטת מותג ולא באג — לכן לא בוצע.
