# TODO — מה שנשאר פתוח

**סטטוס: דחוי.** כרגע העבודה מתמקדת בעיצוב, בסטודיו ובבקאנד.

| | סעיף | |
|---|---|---|
| **C** | חיוב | אין סליקה ואין חשבונית — הפער הפתוח היחיד שנשאר |
| **D4** | ממשק ניהול הזמנות פנימי | תצוגה קיימת; ניהול שוטף לא — וזה מה שחוסם את ההודעות ללקוחה (D2) |
| ~~**B**~~ | ~~ההזמנה עצמה~~ | ✅ נסגר — טבלת `orders`, פירוט מחיר מהשרת, קישור לגרסה שהוזמנה, קבצי ייצור |
| ~~**G**~~ | ~~כניסה מסודרת~~ | ✅ נסגר — גוגל או קוד חד־פעמי במייל |
| ~~**A**~~ | ~~מסלול ההזמנה~~ | ✅ נסגר — מיגרציות, אדמין, התראה, אישור ללקוחה, גיבוי |
| ~~**E**~~ | ~~חשיפת `/debug`~~ · ~~E6 בעלות~~ | ✅ נסגר — שער אדמין, ובעלות על כל מסלול שמקבל מזהה עיצוב |
| ~~**F**~~ | ~~כותרת עברית~~ | ✅ נסגר |

---

# G. כניסה מסודרת — מייל או חשבון גוגל ✅

**נסגר.** הזיהוי עבר ל-Supabase Auth: חשבון גוגל, או קוד חד־פעמי במייל. אין
סיסמה — סיסמה היא עוד דבר לאבד ועוד דבר לאבטח.

- [x] **G1.** `0010_auth.sql` מוסיף `profiles.auth_user_id` → `auth.users(id)`.
      `auth.users` מנהל את **האימות**, `profiles` ממשיך להיות מי שהעיצוב שייך לו,
      ו-`designs.profile_id` לא השתנה — לכן המכסה היומית, רשימת העיצובים וה-FK
      עובדים כמו קודם. `requireAccountId(req)` שמרה על החתימה: המסלולים לא יודעים
      שמשהו השתנה.

> **החלטה שנשתנתה מול התוכנית המקורית: עוגיית הבטא נמחקה, ולא הושארה כמסלול
> מעבר.** `rmjewel_uid` הייתה חתומה אבל נופקה בלי אימות — מי שהזין מייל קיים קיבל
> את החשבון. להשאיר אותה "לתקופת מעבר" פירושו להשאיר בדיוק את החור שהכניסה
> המאומתת נועדה לסגור, ומסלול עוקף שקיים הוא מסלול שנשאר. המחיר חד־פעמי: חבר
> שהיה באמצע עיצוב נכנס שוב, ו-`linkAuthUser` מוצא אותו לפי המייל **עם כל
> העיצובים שלו**. `signIn` הישנה נמחקה מהקוד ולא הושארה כפונקציה לא־בשימוש.

**שלושת הצעדים בקישור** (`linkAuthUser`, ובסדר הזה): לפי `auth_user_id` →
לפי המייל → יצירה. הצעד השני הוא היחיד שאי אפשר לתקן בדיעבד — פרופיל שני יותיר
את העיצובים תלויים בראשון בלי דרך להגיע אליהם — ולכן יש לו טסט משלו.

### תבנית המייל — נדרשת פעולה בקונסולה

**Supabase שולח קישור, לא קוד, עד שמשנים את התבנית.** ברירת המחדל של
"Magic Link" מכילה `{{ .ConfirmationURL }}` בלבד. כדי שיישלח קוד בן שש ספרות
התבנית חייבת להכיל `{{ .Token }}`. הכי טוב להשאיר את שניהם — הקוד עובד גם
ממכשיר אחר, והקישור נוח יותר באותו דפדפן:

```html
<p>הקוד שלך: <strong>{{ .Token }}</strong></p>
<p>או פשוט <a href="{{ .ConfirmationURL }}">לחצו כאן להיכנס</a></p>
```

**הצד שלנו תוקן בנפרד:** `signInWithOtp` לא העביר `emailRedirectTo`, ולכן
הקישור הוביל ל-`Site URL` — דף הבית — במקום ל-`/auth/callback` שיודע להחליף את
הקוד בסשן. משם באמת לא קרה כלום. עכשיו הקישור נוחת ב-callback, ומצב המשפך נשמר
לפני שליחת הקוד ולא רק לפני גוגל: לחיצה על הקישור מרעננת את העמוד בדיוק כמו מסע
OAuth.

**נשאר קוסמטי:** התבניות עדיין באנגלית. שווה לתרגם ולהוסיף `dir="rtl"`.

---

# הזמנה וחיוב

כדי שלקוחה תוכל להשלים הזמנה ולשלם.

## המצב היום (2026-07-28)

**A1 נסגר.** `SUPABASE_DB_URL` הוגדר, ומ-27.7 בשעה 15:15 ה-workflow "Apply
Supabase migrations" עובר בכל push ל-main ומריץ את כל `supabase/migrations/`
בסדר שמות. `inquiries` קיימת בייצור, וגם `0006_reload_schema_cache.sql` —
ה-`notify pgrst` שפתר את השגיאה שנראתה כמו טבלה חסרה אבל הייתה cache.

**A2 נסגר גם הוא** (28.7). מה שנמדד מול הייצור אחרי הפריסה של #68:

```
GET  https://rmjewel.com/api/profiles   → 200
GET  https://rmjewel.com/api/inquiries  → 401  unauthorized     (האדמין פעיל)
```

מה שנשאר בסעיף הזה: **אין שליחת מייל בכלל**. שליחת הזמנה אמיתית לא נבדקה מאז
התיקון — היא כותבת שורה, ולכן זו בדיקה שגל מריץ ולא הסשן.

עמוד `/order` הוסר מהאתר בזמנו כי הטופס שלו מעולם לא עבד; מסלול ההזמנה החי הוא
הצ'קאאוט בסוף משפך `/design` (`submitOrder`).

---

## A. להחיות את המסלול הקיים — חוסם

- [x] **A1. ~~להריץ את `0002_site.sql` בייצור~~** — בוצע. `SUPABASE_DB_URL` של
      ה-**session pooler** (`*.pooler.supabase.com`, IPv4) הוגדר; החיבור הישיר
      `db.<ref>.supabase.co` הוא IPv6-only ו-runners של GitHub Actions נכשלים
      עליו, וזה מה שהפיל את חמש ההרצות שקדמו לו. מכאן כל מיגרציה רצה מעצמה.
- [x] **A2. ~~להגדיר `ADMIN_TOKEN`~~** — בוצע 28.7. `/api/inquiries` מחזיר `401`
      ולא `503`. זה פתח את רשימת הפניות, את לשונית "עיצובים", ואת **E**.
      לזכור לפעם הבאה: `deploy.yml` דוחף סודות ל-Worker רק בזמן פריסה — הוספת
      secret ב-GitHub לבדה לא משנה כלום עד שרצה פריסה.
- [x] **A3. ~~התראה על פנייה חדשה~~** — בוצע. `src/lib/mail.ts` שולח דרך ה-REST
      של Resend (ולא ה-SDK: ה-Worker רץ על runtime של Cloudflare ו-`fetch` מספיק).
      הסוד הוא `RESEND_API` — השם שגל הגדיר — ומתקבל גם `RESEND_API_KEY`, כדי
      שסוד תחת השם השני לא ייראה כמו ספק שלא הוקם.
- [x] **A4. ~~מייל אישור ללקוחה~~** — בוצע, במסלול ההזמנה בלבד. הגוף הוא **אותן
      שורות** שנשלחו אלינו ולא ניסוח שני: היחיד שהיא צריכה לאמת הוא שמה שקיבלנו
      זה מה שביקשה, ושתי גרסאות של אותו סיכום הן שתי הזדמנויות להיבדל.
      `orderRef` נשלח מפורשות מהמשפך ולא נחלץ מגוף ההודעה — regex על טקסט שנבנה
      במקום אחר נשבר בשקט ברגע שמישהו מנסח שורה מחדש.

> **שני כללים שנקבעו כאן.** (1) כשל דואר **לעולם** אינו מפיל פנייה שכבר נשמרה —
> מה שאבד הוא ההתראה, לא ההזמנה; הכיוון ההפוך גורם ללקוחה לשלוח שוב. (2) השליחה
> רצה **בתוך** הבקשה ולא ב-`waitUntil`: בריפו הזה כבר נמדד ש-`waitUntil` לא רץ
> בייצור — `/api/generate` הועבר אליו, כל יצירה נתקעה, ואיש לא ידע כי לא היה מי
> שיכתוב את השגיאה.
- [x] **A5. ~~גיבוי כשה-API נכשל~~** — בוצע. כשהשליחה נכשלת נבנה `mailto:` עם
      **אותן שורות** שנשלחו ל-API, פלוס שם/טלפון/מייל, ומוצע כקישור שני מתחת
      לשגיאה. **בלי הפניה אוטומטית** בכוונה, בשונה מ-`/contact`: מי שאין לו לקוח
      דואר מוגדר בדפדפן היה מאבד גם את המסך שמילא. הבחירה נשארת אצל הלקוחה.

## B. תוכן ההזמנה ✅

**נסגר** (28.7, migration `0011_orders.sql`). הזמנה היא ישות משלה ולא שורה
בטבלת טופס־צור־קשר, ולכן שלושת הסעיפים כאן נסגרו יחד — הם היו תסמין אחד.

- [x] **B1. ~~השרטוט לא נשלח~~** — ההזמנה נושאת `design_id` **ו-`version_id`**,
      ומלשונית "הזמנות" בבק־אופיס יורדים ה-DXF וה-SVG בלחיצה. הגרסה שנשמרת היא
      זו **שהוזמנה**, לא "הנוכחית": אחרי ההזמנה הלקוחה יכולה להמשיך לערוך, ומה
      שנחתך חייב להיות מה שהיא אישרה. הקובץ עצמו **לא** מצורף למייל בכוונה —
      כתובת חתומה חיה שעה, ומייל נקרא גם מחר; ההתראה נושאת קישור לבק־אופיס,
      שמנפיק קישור טרי על פי דרישה.
- [x] **B2. ~~פירוט מחיר~~** — `priceOf` עבר מ-`model.ts` ל-`src/lib/pricing.ts`,
      והשרת מחשב את המחיר בעצמו מאותה פונקציה שהמסך מציג. הפירוט המלא
      (`base/widthAdd/complexity/shipping/total/vat`) נשמר על ההזמנה
      ונשלח בשני המיילים. עד עכשיו מה שנרשם היה מה שהדפדפן אמר — כלומר מספר
      שאפשר לזייף, וגם כשאיש לא מזייף, מספר שאינו קשור למחירון שיחול מחר.
- [x] **B3. ~~הזמנה היא לא "פנייה"~~** — טבלת `orders` עם צינור סטטוסים
      (`sent → approved → in_production → shipped`, ו-`cancelled` בצד),
      `status_history` לכל מעבר, פרטי לקוחה וכתובת כצילום ברגע ההזמנה, וקישור
      ל-`designs`/`design_versions`. **ההזמנות ההיסטוריות לא הומרו**: חילוץ
      מידות ומחיר מטקסט חופשי בעברית הוא ניחוש שנשמר כאילו הוא נתון. הן נשארות
      ב-`inquiries` ונראות בלשונית "פניות" תחת סינון "הזמנות".

## C. חיוב

- [ ] **C1. לבחור ספק סליקה.** ישראלי (Tranzila / Cardcom / PayPlus / Meshulam) או
      Stripe. משפיע על כל היתר בסעיף הזה.
- [ ] **C2. דף תשלום מתארח (hosted).** `CheckoutScreen` לא אוסף פרטי כרטיס — החלטה
      מודעת ומתועדת בקוד, כי איסוף מספרי כרטיס באתר בלי ספק מחובר הוא חשיפה
      אמיתית. המעבר צריך להיות לדף של הספק, לא לשדות כרטיס משלנו.
- [ ] **C3. Webhook לאישור תשלום** → עדכון סטטוס ההזמנה (תלוי ב-B3).
- [ ] **C4. חשבונית/קבלה.** חובה חוקית. ספק חשבוניות (Morning / iCount / חשבונית ירוקה)
      או הנפקה ידנית — צריך להחליט.
- [x] **C5. ~~שיעור המע"מ~~** — נסגר 29.7 בהחלטת גל, ולא רק כשינוי שיעור:
      **המחירים כוללים מע"מ** ואינם נטו שמוסיפים לו אחוזים. `src/lib/pricing.ts`
      מחזיק עכשיו מחיר סטנדרטי כולל (צמיד ₪399, טבעת ₪299 — לפני משלוח, כולל
      אריזה), המשלוח מתווסף, והמע"מ (18%) **נגזר מתוך** הסכום (18/118) כשורת
      מידע. זה גם מה שנדרש מול צרכן: המחיר המוצג הוא מה שמשלמים. המחירים לא
      הועלו כדי לפצות על המעבר מ-17% ל-18% — ההפרש נבלע.
- [ ] **C6. מדיניות ביטול והחזרה.** `disclaimersTitle` כבר אומר "ייצור לפי הזמנה —
      לא ניתן להחזרה". לוודא שהניסוח עומד בחוק הגנת הצרכן לגבי מוצר מותאם אישית,
      ושהוא עקבי בין `/terms` לבין מסך הסיכום.

## D. תפעול אחרי ההזמנה

- [x] **D1. ~~מסך אדמין להזמנות~~** — לשונית "הזמנות", ראשונה וברירת מחדל.
      לכל הזמנה: מספר, לקוחה, מה הוזמן, פירוט המחיר, וסטטוס שאפשר להזיז.
- [ ] **D2. מעקב סטטוס ללקוחה — נבנה, ואז כובה בכוונה (29.7).** המימוש היה
      מלא: כל מעבר שלח מייל בעברית, רק על מעבר אמיתי (קליק כפול או רענון לא
      מודיעים פעמיים), והמסך אמר אם המייל יצא. גל כיבה אותו, וזה הסדר הנכון:
      **הודעה אוטומטית שווה בדיוק כמו הסטטוס שהיא מדווחת עליו.** את הסטטוסים
      אי אפשר לתחזק ברצף מתוך תפריט על כרטיס — לשם כך צריך **D4** — והודעה
      שמבטיחה "נכנס לייצור" על שדה שמתעדכן כשנזכרים גרועה משתיקה: היא הופכת
      מידע ישן להתחייבות. מה שנשאר במקום: הנוסח (`orderStatusMail`, בדוק
      בטסטים) והסטטוסים עצמם. מה שחסר הוא רק החיווט, והוא חלק מ-D4.
- [x] **D3. ~~ייצוא לייצור מההזמנה~~** — כפתור "קבצי ייצור" על שורת ההזמנה,
      דרך `POST /api/admin/designs/<id>/export` עם ה-`versionId` של ההזמנה.
      גרסה שנכשלה בוולידציה נשארת חסומה גם כאן: קובץ שאי אפשר לחתוך שאפשר
      להוריד הוא כשל שעובר לרצפת הייצור.

## D4. ממשק ניהול הזמנות פנימי — התנאי להפעלת D2

לשונית "הזמנות" היום היא **תצוגה**: היא מראה מה הוזמן, מאיפה למשוך את הקובץ,
ומאפשרת להזיז סטטוס בתפריט. זה מספיק כדי לראות הזמנה, ולא מספיק כדי **לנהל**
אותן — ובלי ניהול שוטף אין משמעות להודעה אוטומטית ללקוחה, כי היא תדווח על שדה
שלא מתוחזק. לכן D2 תלוי בסעיף הזה ולא להפך.

מה שחסר, לפי מה שהזרימה היומית באמת דורשת:

- [ ] **D4.1. מסך הזמנה בודדת** (`/admin/orders/<id>`) ולא כרטיס ברשימה: כל
      הפרטים, היסטוריית הסטטוסים (`status_history` כבר נשמר), הערות, והקבצים —
      במקום אחד שאפשר לשלוח אליו קישור ולחזור אליו.
- [ ] **D4.2. תור עבודה ולא רשימה כרונולוגית.** "מה מחכה לי היום" הוא השאלה
      שנשאלת בפועל: מה אושר וממתין לחיתוך, מה בייצור יותר מ-X ימים, מה נשלח
      ולא נסגר. הסינון הקיים לפי סטטוס הוא הבסיס, לא התשובה.
- [ ] **D4.3. הערות פנימיות במסך.** השדה (`note`) והמסלול (`PATCH`) כבר קיימים
      ואינם חשופים ב-UI — זה מה שהופך "עדכנתי סטטוס" ל"למה".
- [ ] **D4.4. פעולה מרוכזת** על כמה הזמנות (למשל להעביר לייצור אצווה שנחתכת
      יחד), אחרת כל מעבר הוא תפריט נפרד וזה בדיוק מה שגורם לסטטוסים להישאר ישנים.
- [ ] **D4.5. ורק אז — לחווט את `orderStatusMail`** (D2), עם אישור לפני שליחה:
      ההודעה יוצאת ללקוחה ואין לה undo.

---

## סדר מומלץ

**A, B ו-D סגורים.** המסלול עובד מקצה לקצה ובשני הכיוונים: הלקוחה מזמינה,
ההזמנה נשמרת כישות עם מחיר מפורט וקישור לגרסה שהוזמנה, גל מקבל התראה, הלקוחה
מקבלת אסמכתה, גל מזיז סטטוס והיא מקבלת עדכון, וקבצי החיתוך יורדים מאותה שורה.

**גם G סגור** — הכניסה מאומתת (גוגל או קוד חד־פעמי במייל).

מה שנשאר הוא **C** בלבד, וכל סעיף פתוח בו הוא החלטה של גל ולא קוד: C1 בחירת
ספק סליקה, C4 בחירת ספק חשבוניות, C6 ניסוח מדיניות הביטול. **C5 נסגר** —
המחירון כולל מע"מ (₪399 צמיד, ₪299 טבעת, לפני משלוח) ו-18% נגזרים מתוכו.
**C3** (webhook שמעדכן סטטוס) כבר לא חוסם את עצמו: צינור הסטטוסים קיים, וכל
מה שנדרש הוא מי שיקרא לו.

---

# E. לסגור את `/debug` — חשיפת מידע לקוחות ✅

**נסגר.** עד עכשיו, בלי שום אימות, כל מי שידע את הכתובת קיבל:

```
GET /api/debug/log                       → 200, 80 הרצות
GET /api/debug/log/<id>                  → 200, 288KB פירוט מלא
GET /api/debug/log/<id>/image/render     → 200, PNG של 1.7MB
```

מתוך 80 ההרצות, **15 הכילו טקסט חופשי שלקוחות כתבו** — למשל
`"תיאור הלקוחה: עלים המחוברים אחד לשני, רק קווי המתאר שלהם..."` — יחד עם
הפרומפט המלא, ההדמיות, זמני ריצה ושם המודל.

- [x] **E1. `requireAdmin(req)` על ארבעת המסלולים.** ב-`api/debug/run` הבדיקה
      יושבת **לפני** ה-`try`, לא בתוכו: ה-`catch` שם כותב שורת הרצה על כל כשל,
      ולכן שער בתוך ה-`try` היה נותן לזר לכתוב למסד בכל בקשה נדחית.
- [x] **E2. שער על `/debug`** — `AdminGate` ב-`src/components/admin/AdminGate.tsx`,
      אותה עוגיית `forme_admin` של `/admin`. נוסף `GET /api/admin/session`
      ("האם אני מחובר?") כדי שהעמוד יוכל להציג טופס כניסה בלי למשוך קודם את
      המידע שהוא מגן עליו. טופס הכניסה הוצא מ-`AdminDashboard` ומשותף לשניהם —
      שני שערים שנראים אחרת הם שני שערים שמתחזקים אחרת.
- [x] **E3. `Disallow: /debug` ב-`robots.ts`.** הקשחה משנית; E1 הוא ההגנה.
- [x] **E4. סריקה אחרי התיקון** — הפכה מ-grep חד־פעמי לטסט
      (`src/lib/__tests__/routeGuards.test.ts`) שנכשל אם ייכתב מסלול debug חמישי
      בלי שער. הבדיקה החד־פעמית לא הייתה מונעת את החזרה של אותו באג.
- [x] **E5. `/api/llm-health` נסגר גם הוא** — ממצא מהסריקה. לא בגלל מה שהוא
      מחזיר, אלא כי **כל קריאה אליו פונה ל-OpenAI עם המפתח שלנו**: נקודה
      לא־מאומתת שכל אדם ברשת יכול לדפוק בה על חשבון המפתח.

## E6 — בעלות על מסלולים שמקבלים מזהה עיצוב ✅

- [x] **E6.** מזהה עיצוב אינו סוד — הוא עובר בכתובות, ביומנים ובקישורים — ואף
      מסלול לא בדק אם הפונה הוא הבעלים. מי שהחזיק uuid יכול היה לקרוא עיצוב של
      חבר, לשנות אותו, למחוק אותו, לשכפל, לייצא, ו**להריץ עליו יצירה**: הרצת
      מנוע שנספרת על המכסה של בעליו ועולה לנו כסף. נסגרו שבעה מסלולים —
      `designs/[id]` (GET/PATCH/DELETE), `choose`, `duplicate`, `generate`,
      `generate/[jobId]`, `vectorize`, `export`.
- **ההכרעה** (`src/lib/designAccess.ts`) היא מראה של זו שביצירה, מהכיוון ההפוך:
      ביצירה הבעלים נקבע מהבקשה, כאן הוא כבר כתוב בשורה. לכן הלקוח **לא** שולח
      `profileId` — כל פרמטר שהדפדפן שולח הוא פרמטר שאפשר לזייף. עיצוב של פרופיל
      בודק נשאר פתוח (זה הסטודיו הפנימי, שאין לו חשבון); כל השאר דורש שהעוגייה
      החתומה תצביע על אותו פרופיל בדיוק.
- **תופעת לוואי מכוונת:** חבר שמנקה עוגיות מאבד גישה לעיצובים שלו, גם אם הם
      רשומים אצלו מקומית. זו המשמעות של זהות שנשענת על עוגייה בלבד — וזה בדיוק
      מה ש-**G** סוגר.

---


# F. כותרת עברית ✅

**נסגר.** `he.site.tagline` (`"Precision jewelry, formed around you."`) היה
הכותרת **היחידה** של אתר `lang="he" dir="rtl"` — בתוצאת חיפוש עברית ובשיתוף
בוואטסאפ לא הופיעה מילה אחת בעברית.

- [x] **F1.** נוספה `he.site.titleHe` = `"RM JEWEL — תכשיטים בעיצוב אישי"`,
      והיא משמשת ב-`<title>` של השורש ושל קבוצת האתר, ב-`og:title`,
      ב-`twitter:title` וב-`manifest.name`. `tagline` **לא נגעו בו** — הוא נשאר
      הטאגליין האנגלי של המותג בפוטר, וזה המקום היחיד שהוא מופיע בו.
- [ ] **F2. האם `brandHe` ("אר. אם. ג'ואל") נכנס לכותרת?** נשאר פתוח — זו החלטת
      מותג, לא באג. אם התשובה כן, זו מחרוזת אחת (`titleHe`) ותו לא.
